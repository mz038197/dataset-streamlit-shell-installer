# Dataset Streamlit Shell

教學用 Streamlit 資料實驗室殼層：自雙表起點經整合與整理 Working，圖表探索／資料切分／PCA 讀 Working，並以內建範例走通監督式與非監督式演算法頁；另含 AI 新創工作坊的專案展示軌道。

## Language

**AI新創工作坊**:
側欄導覽分段名稱；與「降維分析」同層。底下目前僅「專案展示」一頁。
_Avoid_: Startup Challenge（作側欄分段名）、挑戰區、展示區

**專案展示**:
AI新創工作坊下的側欄頁正式名稱。學生工作流：選挑戰公司後瀏覽該公司資料、清理 Challenge 工作資料，再切成 Challenge 訓練資料與 Challenge 測試資料；下半為模型區與成果區空輪廓，討論後由 Agent 以 AI coding 補齊。不是重做整條 ML 教學實驗室，也不是上台講稿白板。頁內主標為「專案展示」。
_Avoid_: Startup Challenge 展示頁（舊草案頁名）、展示空頁（備課狀態口語）、以「成果展示」作側欄名、上台白板（口語）、白板三塊（舊頁結構）

**白板三塊**:
已廢止的舊頁結構：①我們在解決什麼？②我們做出來的結果？③我們不能亂承諾什麼？專案展示改為資料瀏覽＋模型區＋成果區後不再使用。
_Avoid_: 當作現行頁結構；用①②③當區塊標題

**模型區**:
專案展示下半空輪廓之一（框始終可見）。內容為選型與訓練（模型名稱、必要旋鈕、開始訓練），不含成果圖表。無 Challenge 訓練資料與 Challenge 測試資料時只顯示空輪廓、不可填入。Agent 可一次寫入本區與成果區程式。
_Avoid_: 白板②、我們做出來的結果；把指標圖／混淆矩陣放進本區；沒檔就隱藏下半；寫死分數當訓練結果

**成果區**:
專案展示下半空輪廓之二（框始終可見）。內容為訓練後的指標、圖與一次演示。無訓練／測試檔、或尚無 Challenge 模型產物時只顯示空輪廓。不是側欄名。
_Avoid_: 以「成果展示」作側欄名、白板③、我們不能亂承諾什麼（不當本區標題）；寫死分數常數；沒檔就隱藏下半

**專案展示空殼**:
老師發佈時的 `ui/startup_challenge_ui.py`：上半 Challenge 資料檢視，下半模型區／成果區空輪廓，並依「有無訓練／測試檔、有無 Challenge 模型產物」決定顯示輪廓或填入內容。某挑戰公司尚無 Challenge UI 快照時載入此檔；清除回起點確認後亦載入（該公司快照已刪）。不還原其他教學頁。
_Avoid_: 還原整份學生專案；更換挑戰公司時有快照仍蓋成空殼；讓 Agent 拆掉無檔則顯示輪廓的判斷；讓 Agent 自行還原空殼

**Challenge UI 快照**:
該挑戰公司上次的專案展示 live UI 副本（`workspace/challenge/{company}/startup_challenge_ui.py`）。更換挑戰公司時先把目前 `ui/startup_challenge_ui.py` 存成原公司快照，再載入新公司快照；沒有則用專案展示空殼。重整頁面不改 live UI。Agent 只改 live UI，不直接改各公司快照。清除回起點確認會刪目前公司這份快照，並把 live UI 還原成專案展示空殼。
_Avoid_: 換公司一律還原空殼；五間公司共用一份已填模型區的 UI 去配另一間的資料；把快照當學生主編檔；清除回起點仍留該公司快照

**Challenge 模型產物**:
模型區訓練成功後、成果區用來判斷可以渲染的產物。各挑戰公司各自一份，寫在該公司資料夾；重整頁面與更換挑戰公司後仍有效。沒有產物時成果區維持空輪廓。該公司的 Challenge 訓練資料／測試資料被刪除時一併失效。
_Avoid_: BOARD_METRIC_LINE 寫死分數；把教學頁 artifact 當挑戰產物；五間公司共用一個產物標記

**挑戰公司**:
專案展示的五選一委託情境識別：`edupulse`／`vitalrisk`／`airsense`／`churnlab`／`flowcast`；決定 Challenge 起點資料、資料說明書與倫理加碼。頁上已確認的那一間在重整頁面後仍有效，不回到清單第一間。
_Avoid_: 把公司 id 與側欄頁名混稱；混用其他公司的 CSV；重整後當成沒選過而跳回 edupulse

**更換挑戰公司確認**:
更換挑戰公司生效前的模態確認。選到另一間必須通過；進頁第一次或選同一間不跳。例外：磁碟上還沒有已確認的挑戰公司、卻仍有舊的共用 working／train／test 時，第一次也要確認（即使選清單第一間），確認後才把舊檔搬進該公司資料夾。取消或關閉則維持原公司。確認後切到該公司的資料、Challenge UI 快照與 Challenge Agent session；不會刪除各公司已分開存放的檔。再切回來進度仍在。
_Avoid_: 防呆、alert、window.confirm；只在有工作資料時才跳；確認更換就刪掉工作副本；文案仍寫會清除工作資料；有舊共用檔時默默搬進清單第一間

**清除回起點確認**:
專案展示「清除回起點」生效前的模態確認。只動目前已確認的挑戰公司：刪 Challenge 工作資料／訓練資料／測試資料與該公司 Challenge UI 快照，Challenge 模型產物失效，live UI 還原成專案展示空殼；Challenge 起點資料、其他公司與 Challenge Agent session 不動。無工作／切分／產物且無該公司快照時不畫按鈕。取消或關閉維持現狀；確認後 Challenge 資料檢視改看起點。
_Avoid_: 重置、防呆、alert、window.confirm；清除訓練／測試（未刪 working 的舊草案）；回到只有工作檔案；與清除回雙表起點混稱；當更換挑戰公司；清對話；五間一起清；兩個確認視窗

**Challenge 起點資料**:
各挑戰公司內建的只讀起點 CSV（`workspace/challenge/{company}.csv`）。可髒、供診斷與複製清理；不得覆寫此檔。與雙表線的 Original 原始資料不是同一概念。
_Avoid_: Original 原始資料（未加 Challenge 前綴）、起點檔（未鎖定挑戰語境時）

**Challenge 工作資料**:
學生清理後的工作副本；各挑戰公司各自一份（`workspace/challenge/{company}/working.csv`），從不覆寫該公司的 Challenge 起點資料。切出該公司 Challenge 訓練資料／Challenge 測試資料的來源；模型不直接吃這份。任何寫回該公司本檔都會刪除該公司已有的訓練／測試檔（切分作廢）。與雙表線根目錄的 Working 工作資料不是同一份。不走 Ready，亦不強制 cleaning_log。重整頁面與更換挑戰公司時保留；清除回起點確認後刪除該公司這份。安裝或更新殼層時不得覆蓋或刪除各公司工作資料、訓練資料、測試資料，以及舊的共用 working／train／test。
_Avoid_: Working、working.csv（未指明挑戰公司與軌道）；五間公司共用一份工作副本；更換挑戰公司或重整時刪掉工作副本；安裝／--force 蓋掉學生已清的 Challenge 工作資料；暗示挑戰線要先建立 Ready；把切分寫到根目錄 train／val／test

**Challenge 訓練資料**:
從該公司 Challenge 工作資料切出的訓練 CSV（`workspace/challenge/{company}/train.csv`）。由學生與 Agent 討論後寫出，頁上無套用按鈕。預設 80／20；有類別目標則分層，否則隨機。模型訓練吃這份。不是側欄「資料切分」的產物，也不是根目錄 `train.csv`。更換挑戰公司時保留；寫回該公司 Challenge 工作資料時一併清除；清除回起點確認後亦刪。
_Avoid_: 資料切分（側欄頁）、Ready、workspace/train.csv、Challenge 工作資料（當訓練輸入）；挑戰線 val；五間公司共用一份 train.csv

**Challenge 測試資料**:
與該公司 Challenge 訓練資料同一次切分寫出的測試 CSV（`workspace/challenge/{company}/test.csv`）。模型評估／演示吃這份。不是根目錄 `test.csv`；挑戰線第一版不做 val。更換挑戰公司時保留；寫回該公司 Challenge 工作資料時一併清除；清除回起點確認後亦刪。
_Avoid_: workspace/test.csv、Challenge 驗證資料（第一版不做）、側欄「資料切分」的 test.csv；五間公司共用一份 test.csv

**Challenge 資料說明書**:
各挑戰公司給學生的欄位與委託說明（`workspace/challenge/{company}_資料說明書.md`）。解釋欄位與目標前應先讀；教師用缺陷說明不進學生專案。不印在專案展示頁上。
_Avoid_: 教師用缺陷說明、劇透缺陷清單；把說明書全文貼進主教學欄

**Challenge 資料檢視**:
專案展示上半切換目前查看哪一份挑戰 CSV 的控制。標籤為起點／工作／訓練／測試；有檔才可選。有 Challenge 工作資料時預設工作，否則起點。起點只讀。「清除回起點」放在此控制旁；通過清除回起點確認後改看起點。
_Avoid_: 一次並排四份表；預設改成訓練資料；把根目錄 train／test 算進來；把清除回起點放到挑戰公司下拉或模型區

**dataset_base_context**:
總覽、AI 協作資料整理與降維分析專用的 Agent `host_context`。內容為雙表工作副本、Original、Working、腳本目錄與 cleaning_log 規則。不含教學頁寫回規則，亦不含 Ready。
_Avoid_: 焊進類神經網路左欄或線性回歸決策槽；教學頁沒傳 host 就吃這包；與 Challenge host context 或教學頁 host context 叠用；引導建立 ready.csv

**教學頁 host context**:
監督式、非監督式、深度學習、電腦視覺各頁自己的 Agent `host_context`，不叠 dataset_base_context。本頁用內建範例資料，不准動根目錄 Original／Working，不要讀寫 ready.csv；有寫回協定的頁另接該頁規則。每一輪頁面狀態不叠根目錄 Working 列數／欄位。
_Avoid_: 沒傳 host 就吃 dataset_base_context；在圖表探索組線性回歸或改 NN 架構；把 Challenge host context 當教學頁 host；跟學生講左欄下拉

**Challenge host context**:
專案展示頁專用的 Agent `host_context`：定角色、挑戰軌道檔案邊界（起點只讀、目前公司的工作副本可寫、訓練／測試由該公司工作副本切出、改該公司 working 即作廢該公司切分）、模型區／成果區完成樣貌，並串上當前挑戰公司的倫理／資料加碼片段；不叠加 dataset_base_context，也不叠教學頁 host context。加碼只強化必講紅線與檢查方向，不剧透教師缺陷清單。倫理紅線只在對話與口頭 Gate，不上頁。禁止拆掉專案展示空殼「無檔則顯示輪廓」的判斷。還原空殼只走清除回起點確認，Agent 不得自行還原。
_Avoid_: 挑戰頁直接叠 Titanic／Ready／教學頁寫回；靠「衝突以挑戰為準」口頭覆寫卻仍灌入根目錄 working 規則；在 host 列出老師埋的缺陷清單；引導去改根目錄 train／val／test；把紅線寫成頁上第三塊；引導改其他公司資料夾或 Challenge UI 快照；自行還原專案展示空殼

**Challenge Agent session**:
專案展示頁專用的對話 session；與雙表整理線的 Agent session 分開。各挑戰公司各一條；更換挑戰公司時切換 session、不清空對話。重整頁面後不保留（不寫盤）。進頁或換公司時以該公司的 Challenge host context 對應該條 session。清除回起點確認也不清空該公司對話。
_Avoid_: 與全站整理頁共用同一條 session 卻不換 host；換公司就重建而砍掉該公司對話；清除回起點就清對話；只靠每輪 snapshot 提醒卻不換 host；為挑戰頁單獨做 F5 對話存盤

**Challenge 允許改動範圍**:
專案展示軌道上 Agent／學生預設可改：`ui/startup_challenge_ui.py`、目前挑戰公司資料夾內的 Challenge 工作資料／訓練資料／測試資料、必要時 `scripts/`。不改其他 ML 教學頁、不改專案展示空殼、不直接改各公司 Challenge UI 快照；頁入口薄包裝與側欄導覽由老師預放。
_Avoid_: 為挑戰去改邏輯回歸等教學頁；把挑戰成果寫進根目錄 working／ready／train／val／test；改其他公司資料夾或 empty_shell

**Challenge 上台 Gate**:
人審／自評的最小可上台規準（能講委託問題、有 Challenge 訓練資料／測試資料並用它們訓練、成果區可演示、含該公司必講紅線、分得清 Agent 與人類決策）。第一版不做機器硬檢查；每組自選獨特畫面為軟規則。不印在專案展示頁上。
_Avoid_: 第一版就做 Gate 自動驗收；把獨特畫面做成硬檢查；頁上長 checklist；仍以白板三塊當規準文案

**主教學欄**:
內容區左側的教學主體區；承載頁標題、學習階段與演算法互動。與 Streamlit 左側導覽側欄不同。
_Avoid_: 中欄、中間欄位（未鎖定全站殼時）、主欄（易與線性回歸頁內「模型」欄混淆）

**資料 Agent 欄**:
內容區右側的資料 Agent 協作區；與主教學欄並排，寬度可調時指此欄。
_Avoid_: 右欄、右邊欄位（未鎖定全站殼時）、側欄（本專案「側欄」專指導覽）

**思考過程**:
資料 Agent 欄裡、附在該則助理回覆上的推理摘要；在答案出現前以及每一輪工具呼叫之間可看。不是最終回覆，也不屬對話紀錄檔。
_Avoid_: 思考輸出、推理過程、右欄思考、把推理深度當同義（Studio 設定項，本殼第一版不做）

**語音設定**:
已隱藏。資料 Agent 欄不再顯示語音 expander，也不播放。`user_settings.json` 的 tts 鍵可留在磁碟，殼不拿來驅動畫面或播放。
_Avoid_: 語音（當現行資料 Agent 欄功能）；把隱藏當成刪除設定檔

**建議問句**:
已廢止的複製提問區塊（舊標題「建議問 Agent」）。曾出現在主教學欄或圖表探索的資料 Agent 欄。全站不再使用；卡住時改走訓練前預測旁的「Agent 提示」。
_Avoid_: 建議問 Agent（當現行區塊）；跟「Agent 提示」混稱；再加回主教學欄或資料 Agent 欄

**內容區雙欄殼**:
全站內容區的版面契約：主教學欄與資料 Agent 欄並排；內容區左右內邊距對稱且緊湊（1rem），讓主教學欄貼近導覽側欄；主教學欄在視窗高度內獨立縱向捲動，資料 Agent 欄釘在視窗內；欄內僅訊息列表自捲，`chat_input` 釘在欄底；送出對話只重跑資料 Agent 欄，不重跑主教學欄；附圖不在第一版 Agent 欄。資料 Agent 欄寬可拖曳調整並全站共用同一記住寬度。不含 Streamlit 左側導覽側欄，亦不涵蓋線性回歸頁內的決策槽列。
_Avoid_: 三欄殼（易與 waku 左 nav 或舊回歸三欄混淆）；側欄雙欄；把導覽側欄算進本殼；把側欄變窄當成縮距離；只縮一邊內邊距；Agent 欄整欄第二條捲軸；輸入浮在訊息流中間；送出對話時整頁變暗等待

**側欄品牌列**:
導覽側欄頂部的品牌列；左為 logo、右為固定字樣 `VansCoding.AI`。視覺對齊 classroom 的品牌列。不是頂部 Deploy chrome，也不屬內容區雙欄殼；側欄收合時頂部不出現字樣。
_Avoid_: header（未加「側欄」易與頂部 chrome 混淆）、頂欄、導覽列、把 classroom 的「凡思課堂安裝」當本殼字樣

**K-近鄰分類**:
監督式分類教學頁的正式名稱；以距離找 k 個鄰居、多數決決定類別的實例型（懶惰）學習。側欄插在線性支持向量機與決策樹與集成之間；僅使用內建範例資料（不提供 ready.csv 切換）。頁內不再分學習階段。採訓練前預測殼（兩題解鎖＋Agent 提示），解鎖後主按鈕為「開始預測演示」，以「下一步」走預測過程演進；不做類神經網路式實驗迴圈。
_Avoid_: KNN 分類（可作副標／口語）、K 最近鄰、k-NN、KNN 回歸；本頁主按鈕「開始訓練」；本頁學習階段 radio

**決策樹與集成**:
監督式分類教學頁的正式名稱；由單顆決策樹走到隨機森林再到 XGBoost，帶出 Bagging 與 Boosting 差異。側欄單一頁（併入原獨立 XGBoost 頁）；頁內三個學習階段；僅使用內建範例資料。
_Avoid_: 決策樹概念（舊側欄名）、側欄分「決策樹概念」「XGBoost」兩頁、樹模型（作頁標題）

**學習階段（決策樹與集成）**:
頁內用 horizontal radio 切換的教學段落（不用 tabs）。標籤依序為「單顆決策樹」「隨機森林（Bagging）」「XGBoost（Boosting）」。Bagging vs Boosting 對照以階段3開頭的對照區塊為主，並以訓練前預測題強制區分「平行多數決 vs 序列糾錯」；不另開第四階段。
_Avoid_: Tab、分頁（指 st.tabs 實作）；階段標籤只寫演算法名而不標 Bagging／Boosting；另開第四段專講 Bagging vs Boosting；只靠走過兩段自行領悟、無對照文／無關卡

**Bagging（決策樹與集成）**:
多棵樹大致平行訓練、再彙總（如多數決）的集成策略；本頁以隨機森林為教學承載。
_Avoid_: 把 Bagging 當獨立側欄頁；第一版深講 bootstrap 抽樣公式細節當主菜

**Boosting（決策樹與集成）**:
多棵弱學習器序列訓練、後者針對前者錯誤調整的集成策略；本頁以 XGBoost 為教學承載。
_Avoid_: 把 Boosting 當獨立側欄頁；第一版深講二階導數／自訂 loss 當主菜

**訓練前預測（決策樹與集成）**:
解鎖主按鈕前的概念關卡；三個學習階段各兩題。階段1：分裂／不純度；階段2：平行訓練與多數決（Bagging）；階段3：與 Bagging 對照的序列糾錯（Boosting）。
_Avoid_: 僅階段3有關卡；硬鎖「必須先跟 Agent 說話才能訓練」

**內建範例資料（決策樹與集成）**:
階段1 使用小筆數玩具 CSV（可手算熵／資訊增益）；階段2 與階段3 共用同一份較完整的分類 CSV（現行為心臟病資料），使 Bagging vs Boosting 差異不歸因於換資料。皆不提供 ready.csv 切換。
_Avoid_: ready.csv 作為本頁資料來源；三階段各用不同主資料；全程只用 10 筆玩具跑隨機森林／XGBoost

**隨機森林（決策樹與集成）**:
階段2 的 Bagging 承載；解鎖後以 `n_estimators` 為主旋鈕，並以單顆決策樹 vs 森林的驗證準確率並排對照。特徵隨機／bootstrap 細節可降級補充，不當第一版主菜。
_Avoid_: 第一版主路徑露出 `max_features` 當第二主旋鈕；以投票動畫取代準確率對照當唯一主秀

**XGBoost（決策樹與集成）**:
階段3 的 Boosting 承載；開頭為 Bagging vs Boosting 對照區塊，解鎖後以 `learning_rate` 為單一主旋鈕，搭配訓練／驗證準確率。`n_estimators` 固定合理預設。one-hot 與訓練／驗證切分可靜默沿用。舊獨立頁的雙超參數掃描圖與 early stopping 敘事不做第一版主路徑。
_Avoid_: 階段3 主路徑保留 n_estimators＋learning_rate 雙掃描；以 early stopping 當第一版主菜；獨立側欄「XGBoost」頁；階段3 主旋鈕再用 `n_estimators` 與階段2 撞名

**訓練／驗證切分（決策樹與集成）**:
階段2 與階段3 共用同一 80/20 切分與同一 random_state。階段3 結果區顯示同切分下的隨機森林驗證準確率作為 Bagging 對照基線。屬該教學頁內部 holdout，不寫出前處理的 train／val／test 檔，亦非「資料切分」頁。
_Avoid_: 兩階段各切各的當預設；用不同資料切分比較 Bagging／Boosting 分數；與「資料切分」混稱；暗示本頁會改寫 workspace 的 train.csv

**單顆決策樹（決策樹與集成）**:
階段1 承載；沿用現有玩具資料上的熵／資訊增益表、分裂準則與淺層 max_depth、決策樹圖與訓練準確率，並加上該階段兩題訓練前預測。不做本階段驗證切分主路徑。
_Avoid_: 階段1 改成 2D 決策邊界主秀；為集成線先砍掉準則二選一；階段1 主秀改跑心臟病資料

**學習階段（K-近鄰分類）**:
已廢止的舊頁結構：horizontal radio 切「鄰居與投票／選擇 k」，各階段兩題關卡。本頁改為單一節奏：兩題關卡 → 解鎖後 k slider 與預測過程演進。
_Avoid_: 當作現行頁結構；恢復兩階段；本頁再放學習階段 radio

**k（K-近鄰分類）**:
要查詢的最近鄰居個數。關卡解鎖後以奇數步進 slider 設定；按「開始預測演示」當下鎖定該輪，演示進行中改 k 不套用，須重按開始才用新 k。第一版距離固定歐氏、投票為多數決（uniform）；不露 weights／metric 進階項。
_Avoid_: 關卡前露出 k slider；演示中途改 k 立刻重算；第一版強調平票處理；舊兩階段（階段1 隱藏 k、階段2 才出現）

**內建範例資料（K-近鄰分類）**:
兩特徵、二元分類、尺度相近、可畫 2D 的教學用 CSV。標準化固定開啟、不露開關。本頁主路徑不做刻意尺度失衡對照。
_Avoid_: ready.csv 作為本頁資料來源；第一版 multiclass／高維主路徑；本頁主路徑用尺度陷阱資料或標準化開關

**訓練前預測（K-近鄰分類）**:
解鎖主按鈕前的概念關卡；兩題：實例型（無 w）與多數決。解鎖後的主按鈕為「開始預測演示」（非「開始訓練」）。k 過大與尺度主導不當解鎖題。
_Avoid_: 類神經網路式實驗迴圈；硬鎖「必須先跟 Agent 說話才能訓練」；本頁主按鈕沿用「開始訓練」；兩階段共四題；以尺度題當解鎖門檻

**查詢點（K-近鄰分類）**:
用來示範預測的單一座標點（新資料、非訓練列）。先以內建 3 筆示範靠「下一步」進來；3 筆走完後才可以圖上 click 加演。當下這筆在多數決前必須明顯可辨、且不得穿類別色；④ 才改為預測類別色。已預測新點可較低調。本頁採用可點選的互動圖（Plotly），不沿用其他 ML 頁的 matplotlib＋st.pyplot 主路徑。
_Avoid_: 第一版以 confusion matrix／classification report 當主視覺；強制全專案改 Plotly；3 筆尚未走完就用 click 進第一點；投票前用類別色標查詢點；為好找而讓查詢點與訓練點同色

**開始預測演示（K-近鄰分類）**:
本頁解鎖後的主按鈕文案；按下後鎖定 k、fit 鄰居池。圖上此時只有訓練點，不出現查詢點、不畫決策邊界；第一次「下一步」才讓第 1 個查詢點出現。本頁對應區塊標題為「預測演示」。重新按下可清空已標示點與演進狀態後重跑。
_Avoid_: 本頁主按鈕、區塊標題或可見解鎖文案寫「開始訓練」／「##### 訓練」；按下即自動播放或立刻畫出邊界／第一個查詢點；為對齊本頁而改其他 ML 頁按鈕

**預測過程演進（K-近鄰分類）**:
按「開始預測演示」之後、以「下一步」推進的預測儀式（無自動播放、無逐步模式開關）。訓練集固定為鄰居池。內建 3 筆示範查詢點依序進來；每點四拍：①查詢點出現 → ②依距離排序（訓練點填色仍為類別，距離用點大小／透明度，並附前若干名距離表）→ ③只留最近 k 條連線 → ④多數決上色。④ 後連線與表留到下一點的 ① 才清；**鄰居連線只顯示當下這筆**。3 筆逐步期間不畫決策邊界；第 3 點 ④ 後才自動出現。之後 click 為新點的 ①，再用「下一步」走 ②③④，加演時邊界留著。進行中忽略 click、改 k 不套用。示範點與後續 click 點皆累積保留。對齊懶惰學習的預測，不是學權重／降 Cost 的訓練迴圈。
_Avoid_: 訓練過程演進（本頁語意）；time.sleep 自動播；逐步模式開關；偽 epoch／Cost 曲線；對訓練集逐筆預測／leave-one-out 當主秀；距離熱圖蓋掉類別色；逐點比完約 80 筆才取 k；已預測點連線全部留在圖上；3 筆走完前畫決策邊界；click 後清掉示範點；進行中 click 改走新點或改 k 立刻重算；① 就用預測類別色標查詢點

**卷積神經網路（CNN）**:
影像辨識觀念教學頁的正式名稱；以小視窗（kernel）在圖上滑動找局部特徵，再經活化與縮小後分類。敘事以指定觀念主軸影片為骨幹，開頭保留「圖片＝矩陣」前置；互動示範用來驗證觀念，不是實驗迴圈主體。
_Avoid_: CNN 介紹（可作檔名／口語）、Convolutional Neural Network 當頁標題主文、把 inductive bias 專名當第一版主菜

**學習階段（卷積神經網路）**:
頁內用 horizontal radio 切換的教學段落（不用 tabs）。六段依序為：圖片與矩陣；為什麼需要 CNN；卷積與特徵圖；ReLU 與 Pooling；CNN 整條流程；動手做 CNN。
_Avoid_: Tab、分頁（指 st.tabs 實作）；把六段併回「局部只改兩 tab」的舊結構

**觀念主軸影片（卷積神經網路）**:
DataMListic《CNNs - Explained》（video_id `YGILT182T6w`）。階段1不嵌入；階段2～6嵌入同一支片並附該段建議時間戳；正文為繁中整理，影片為對照。
_Avoid_: 影片僅備課不進學生 UI；硬鎖「必須看完整支才能往下」；第一版灌譯 inductive bias 專名

**訓練前預測（卷積神經網路）**:
階段6解鎖「開始訓練」前的兩題概念關卡：kernel／卷積在做什麼，以及 pooling 留下什麼。
_Avoid_: 階段6直接訓練無關卡；把「為什麼不用 Dense」或 ReLU 當解鎖硬門檻；硬鎖「必須先跟 Agent 說話才能訓練」

**史話補充（卷積神經網路）**:
Hubel & Wiesel 與 LeNet-5 等歷史／生物啟發內容；降級為可展開補充，不佔「為什麼需要 CNN」主線。
_Avoid_: 與影片動機並列當階段2正文主軸

**教學流程圖**:
教學頁上把演算法路徑畫成可導覽、可在作動時點亮的節點流程（非側欄頁）。拓撲與節點文案依各頁定義；可複用於多頁，但各頁節點不必相同。
_Avoid_: 整條流程（CNN 舊口語可保留於該頁）；把流程圖當唯一學習階段取代 radio；暗示為可編輯的工作流引擎

**教學流程圖（線性回歸）**:
已廢止的舊頁骨架：三節點 SVG「輸入資料 → 回歸模型 → 輸出呈現」加目前查看。由決策槽列取代。
_Avoid_: 當作現行頁結構；可點舊三節點當選擇明細

**目前查看（線性回歸）**:
已廢止的焦點單欄切換（輸入資料／回歸模型／輸出呈現）。
_Avoid_: 當作現行導覽；與 Challenge 資料檢視的「目前查看」混稱

**訓練微步驟（線性回歸）**:
已廢止的四格步進條（預測 ŷ → 算 Cost J → 算梯度 → 更新參數）與訓練雙欄。
_Avoid_: 當作現行訓練畫面；逐步模式

**梯度演算板（線性回歸）**:
已廢止的訓練期參數級 w／b／∂J 演示。
_Avoid_: 當作現行區塊

**樣本運算表（線性回歸）**:
已廢止的訓練期前 5 筆 ŷ／error 表。不是訓後預測與誤差表。
_Avoid_: 當作現行區塊；與訓後誤差表混稱

**決策槽**:
主教學欄上一格必須選定的模型決策。線性回歸與邏輯迴歸皆用決策槽列，六格依序皆為輸入資料、訓練／測試切分、特徵縮放、線性層、損失函數、優化器。輸入資料進頁即為該階段內建表，不是學生可選的來源。資料 Agent 對學生講各格、請我寫入；輸入資料已完成時可說其餘由我寫入。
_Avoid_: 積木、chip、流程框；把舊三節點「輸入／回歸模型／輸出」叫決策槽；把輸入資料當成進頁後還要選的來源；四個決策槽、五個決策槽（當槽的總數）；下拉選單；✅；叫學生自己選；暗示只有線性回歸有決策槽列；邏輯迴歸第四格改叫輸出層或 Sigmoid 層

**目前選擇**:
某一決策槽當下選定的值。完成時寫在該槽框第二行；未完成時框上只留槽名。完成用綠框、未完成用紅框，不用文字標狀態。輸入資料進頁即完成。訓練／測試切分進頁為尚未選擇。
_Avoid_: 參數（未指明槽）；框上寫尚未選擇或檢視中；把尚未選擇叫預設；把輸入資料的內建表叫預設；用 primary／secondary 當完成狀態；✅；亮起打勾

**選擇明細**:
點決策槽後在列下方展開的只讀說明，顯示目前選擇。輸入資料另含全表列數與訓練欄前 10 列預覽（來源，不是切分後的訓練集）。訓練／測試切分寫入後另含訓練／測試列數。特徵縮放另標公式；損失函數或優化器必要時一行帶過 Cost／GD。邏輯迴歸階段「多項式與 λ」的輸入資料明細另含鎖定的多項式映射 degree=6。不在此改選擇，頁頂也不另掛資料列數 metric。
_Avoid_: 下拉、設定表單；資料明細（另開一塊）；把明細當第二個寫入路徑；恢復獨立「模型公式」段；頁頂四顆 dataset metrics；把輸入資料預覽改成訓練集前 10 列；把 degree 當可改決策

**鎖定槽**:
第一版不可改種類的決策槽。兩頁皆鎖輸入資料（該階段內建表，進頁即完成）、線性層（Dense(1, linear)）、優化器（SGD）。線性回歸另鎖損失函數為 MSE；邏輯迴歸另鎖損失種類為對數損失。可經 Agent 改的有訓練／測試切分的比例、特徵縮放四選一、優化器的 α 與 epochs，以及邏輯迴歸階段「多項式與 λ」損失上的 λ。完成後框上仍寫目前選擇。
_Avoid_: 假下拉；把鎖定槽當成沒有目前選擇；輸入資料框上寫尚未選擇；把線性層／損失／優化器進頁就當完成；把訓練／測試切分進頁就當完成；把 α／epochs 也鎖死；邏輯迴歸線性層改成 Dense(1, sigmoid)

**特徵縮放（線性回歸）**:
決策槽之一。目前選擇為課堂四種方法之一；Agent 組模型時預設寫入 Z分數正規化。同一選擇套用該學習階段訓練集的特徵；μ／max／σ 只從訓練集計算，測試列不參加 fit。第一版沒有「不縮放」。散點用原始特徵尺度，回歸線把縮放後參數映回原尺度。
_Avoid_: 把此槽叫正規化或 Z-score；兩階段永遠 Z-score（舊鎖）；橫軸改成縮放後的 x；用含測試列的全表來 fit

**正規化（除以最大）**:
特徵縮放的一種目前選擇：\(x'=x/x_{\max}\)，\(0\le x'\le 1\) 且 \(x\ge 0\)。框上可寫「正規化」。
_Avoid_: 把資料轉換頁叫正規化；用此名統稱整個特徵縮放槽

**最小-最大正規化**:
特徵縮放的一種目前選擇：\(x'=(x-x_{\min})/(x_{\max}-x_{\min})\)，\(0\le x'\le 1\)。
_Avoid_: Min-Max 當框上主文

**平均值正規化**:
特徵縮放的一種目前選擇：\(x'=(x-\mu)/(x_{\max}-x_{\min})\)，\(-1\le x'\le 1\)。
_Avoid_: Mean normalization 當框上主文

**Z分數正規化**:
特徵縮放的一種目前選擇：\(x'=(x-\mu)/\sigma\)；實務上約 \(-3\le x'\le 3\)。Agent 組模型時的預設目前選擇。
_Avoid_: Z-score 當槽名或頁標題主文（可作口語／公式旁註）；暗示兩階段只能用此法

**損失函數（線性回歸）**:
決策槽之一，且為鎖定槽。第一版目前選擇固定 MSE，不可改。
_Avoid_: 損失 MSE 當槽名；第一版開放 MAE

**優化器（線性回歸）**:
決策槽之一，且為鎖定槽：目前選擇固定 SGD。α 與 epochs 屬此槽，由 Agent 寫入，框上併入目前選擇。
_Avoid_: 另開「訓練設定」；把 α／epochs 當成決策槽以外的旋鈕

**決策槽狀態**:
某一學習階段上，六個決策槽的目前選擇（含訓練／測試切分的比例、優化器的 α 與 epochs，以及邏輯迴歸階段「多項式與 λ」的 λ）。輸入資料進頁即為該階段內建表，載入時也回成該值；其餘由資料 Agent 欄寫入。該階段尚未組過時，訓練／測試切分、特徵縮放、線性層、損失函數、優化器與 α／epochs 為尚未選擇。寫入若改了該階段選擇，該階段訓練結果立刻作廢。
_Avoid_: 保存模型、模型 JSON、stack、form；與訓練結果混稱；改選擇後留舊線；進頁五槽都尚未選擇；兩階段共用一顆切分比例

**模型程式碼預覽**:
由決策槽狀態生成的只讀 Sequential 對應程式。不是可編輯程式，也不是本頁實際 exec 出去的訓練實作。不含訓練／測試切分；切分只出現在該槽的目前選擇與選擇明細。邏輯迴歸的 compile 寫 `BinaryCrossentropy(from_logits=True)`，輸出層為 `Dense(1, linear)`；from_logits 只出現在預覽與模型下載程式碼，不寫進損失函數框上的目前選擇。
_Avoid_: 把預覽當可跑腳本；Agent 另貼一份與預覽不一致的程式；在預覽加 train_test_split；與模型下載程式碼混稱；預覽寫 Dense(1, sigmoid) 又搭配 from_logits=True

**模型下載程式碼**:
依目前學習階段的決策槽狀態生成、可在本頁以外用獨立 Python 專案執行的完整 Sequential 訓練程式。專案含該階段內建表、訓練／測試切分與 fit；Sequential／compile 與模型程式碼預覽同一段，另補切分與 fit。不是本頁訓練實作，頁上不 exec，也不是已訓權重檔。六槽齊即可給（不必過關、不必先訓）；改該階段槽即重生。Keras 預設學習器，切分與頁面同一刀。線性回歸在別處跑完印訓練／測試 MSE 與 w、b；邏輯迴歸印訓練／測試對數損失、正確率、測試集 precision／recall／F1 與 w、b，階段2 另含 φ 與 λ。階段2 的 compile／層寫 `kernel_regularizer=l2(λ)`，並註明與課堂 λ/(2m) 係數可能不同；頁面訓練不 exec 這份。兩頁都是各階段一份，不要打成一包。資料 Agent 可說明存在、何時能下、與預覽／本頁訓練的差別。
_Avoid_: 可攜訓練程式、完整程式、帶走程式、下載模型、保存模型檔、把預覽當可跑腳本、與訓練結果混稱、頁上 exec、沙盒 exec 訓練、寫進 workspace、打包 .venv、必帶 uv.lock、兩階段打成一份、複製教學圖、Agent 另貼程式、宣稱頁面在跑這份

**訓練請求（線性回歸）**:
Agent 在決策槽狀態已齊且訓練前預測已過關時寫入的開訓訊號。系統走與「開始訓練」相同的訓練動畫；Agent 不得自行算完假裝已訓。
_Avoid_: 與決策槽狀態寫成同一件事；未過關或槽未齊仍寫入；沙盒 exec 訓練

**訓練動畫（線性回歸）**:
開訓後主教學欄逐幀更新回歸線、訓練 Cost、測試 Cost、測試集預測對照與一行狀態。Cost 對迭代圖畫兩條線（訓練／測試）。播完前決策槽列可見、框上目前選擇仍在，但不可點；訓練前預測與資料 Agent 欄凍結。播完恢復可點並留下該階段訓練結果。單變量與多變量同一契約。
_Avoid_: 乾淨動畫（舊口語）；訓練微步驟；整頁跟著每一幀重畫；訓練中改決策槽狀態或送出對話；直接出結果而不播；動畫只播訓練 Cost、播完才出測試 Cost；圖上只留訓練一條線；測試集預測對照等播完才出現

**測試集預測對照**:
線性回歸主教學欄裡、回歸線與 Cost 雙圖下方的第三張圖。只畫該階段測試集，點不淡；橫軸實際 y、縱軸預測 ŷ，軸標為「實際 {目標欄}」「預測 {目標欄}」，標題即本詞；加 y=x 虛線（等軸才是 45 度）。軸限鎖在實際 y（兩軸同一範圍，必要時含 0），不跟著爆炸的 ŷ 重算。開訓後與回歸線、Cost 逐幀一起更新；改該階段決策槽即作廢。不是左圖的全表散點。
_Avoid_: 預測結果圖（與訓練結果混稱）；把訓練點畫進本圖；橫軸 ŷ 縱軸 y（已否決）；只在動畫播完才出現；拿本圖取代回歸線或 Cost；本圖測試點淡底；軸限跟著 ŷ 跑

**訓練結果（線性回歸）**:
該學習階段最近一次訓練留下的回歸線、訓練 Cost、測試 Cost、學到的參數，以及測試集預測對照。回歸線由訓練集學出、映回原尺度。單變量左圖為特徵 vs 目標（原尺度）加回歸線；多變量左圖為全表實際 vs 預測。兩階段左圖皆訓練點實心、測試點淡底不同色，測試點不當該散點主秀。改該階段決策槽狀態即作廢；另一階段不受影響。
_Avoid_: 與決策槽狀態混稱；保存模型檔；把模型下載程式碼當成訓練結果；只留一個 Cost 卻暗示已評估測試集；測試點與訓練點同色同透明度；把測試點當左圖散點主視覺；多變量左圖只畫測試集（那是測試集預測對照）

**訓練／測試切分（線性回歸）**:
線性回歸教學頁的決策槽，排在輸入資料之後；各學習階段各有一份。對該階段已鎖定的內建表切成訓練集與測試集。目前選擇為訓練集整數占比（1–99），測試為其餘；0 與 100 拒絕。學生在對話決定數字，資料 Agent 寫入，頁上無旋鈕、無數字框。進頁尚未選擇，沒寫不能訓。「組模型」預設寫入不含此槽，必須另寫。訓練吃訓練集；動畫期間與訓後都同時更新訓練 Cost 與測試 Cost，Cost 對迭代圖為兩條線。只留在該頁狀態，不寫檔。
_Avoid_: 資料切分（槽名；那是前處理側欄頁）、決策欄、訓練／驗證切分、切分比例、訓練比例、train-test split（作頁標題）；寫出 workspace/train.csv；頁上加切分 slider 或％輸入框；挑戰線或 Ready 切分；本頁做驗證集；進頁預設 80／20 且當完成；把切分塞進組模型預設；五檔比例；允許空訓練集或空測試集

**訓練前預測（線性回歸）**:
解鎖訓練前的兩題關卡。單變量：斜率方向、α 過大時 Cost；多變量：為何多 feature、w／b 在學什麼。過關前可寫決策槽狀態，不可訓練（按鈕與訓練請求皆鎖）。Agent 不准代填選項。切分已是必寫槽，關卡不加第三題、也不換掉現有題。
_Avoid_: 改題當第一版主路徑；硬鎖「必須先跟 Agent 說話才能組模型」；為切分加第三題或換掉斜率／α／feature 題

**線性回歸**:
監督式回歸教學頁。側欄單一頁，單變量與多變量兩學習階段，各階段僅用內建範例資料。主教學欄頂部只有決策槽列（無三節點 SVG、無目前查看）；資料 Agent 欄寫入決策槽狀態。輸入資料進頁即為該階段內建表；其餘決策槽進頁為尚未選擇。訓練前預測過關且六槽齊後才可訓練。回歸線與訓練／測試 Cost 固定畫在決策槽列下方，其下為測試集預測對照；訓前空白；開訓走訓練動畫。六槽齊後可給該階段模型下載程式碼。不做本頁保存已訓模型檔／手動預測。
_Avoid_: 側欄分兩頁；欄位下拉；目前查看焦點單欄；輸出呈現當可切焦點；訓練微步驟／梯度演算板／樣本運算表當現行主路徑；把決策槽狀態叫保存模型；把模型下載程式碼叫下載模型；進頁五槽都尚未選擇；跟學生說左欄有下拉或選完亮 ✅

**學習階段（線性回歸）**:
頁內 horizontal radio「單變量」「多變量」：前者餐廳獲利一特徵，後者房價多特徵。各階段自有決策槽狀態、訓練結果與模型下載程式碼，切換不清掉另一側。特徵縮放依該階段目前選擇；散點原尺度、線映回原尺度。
_Avoid_: Tab、分頁（指 st.tabs 實作）；階段名寫成長標題；兩階段共用一份目前選擇；兩階段皆鎖 Z-score；一個下載包打兩階段

**邏輯迴歸**:
監督式二元分類教學頁的正式名稱；側欄單一頁，內含線性邊界與多項式與 λ 兩個學習階段。標籤語意為 `0`／`1`，散點圖例為 `y=0`／`y=1`（非 ±1）；各階段僅用對應內建範例資料。主教學欄頂部為決策槽列（無教學流程圖、無目前查看）；資料 Agent 欄寫入決策槽狀態與訓練請求。輸入資料進頁即為該階段內建表；兩階段都要寫訓練／測試切分。訓練前預測過關且六槽齊後才可訓練。左圖為決策邊界或 contour，右圖為訓練／測試 Cost，其下為測試集混淆矩陣，矩陣下為測試集 precision／recall／F1；訓前空白；開訓走訓練動畫。訓後有分類 threshold 與測試集的 y／ŷ／predicted_class 表。階段1 保留 Sigmoid 函數視覺化。六槽齊後可給該階段模型下載程式碼。不做本頁保存模型 JSON／手動預測，亦無頁首說明 `st.info`。
_Avoid_: Logistic Regression 當頁標題主文；側欄分「邏輯迴歸」「正則化邏輯迴歸」兩頁；圖例寫 y=-1／y=+1；本頁保存／手動預測區塊；頁首長文 info 複述訓練流程；教學流程圖／目前查看／訓練微步驟／梯度演算板／樣本運算表當現行主路徑；頁上 α／epochs／λ 旋鈕；把分類 threshold 當決策槽；把測試集混淆矩陣叫測試集預測對照或測試集機率對照

**教學流程圖（邏輯迴歸）**:
已廢止的舊頁骨架：三節點 SVG「輸入資料 → 分類模型 → 輸出呈現」加目前查看。由決策槽列取代。
_Avoid_: 當作現行頁結構；可點舊三節點當選擇明細

**目前查看（邏輯迴歸）**:
已廢止的焦點單欄切換（輸入資料／分類模型／輸出呈現）。
_Avoid_: 當作現行導覽；與 Challenge 資料檢視的「目前查看」混稱

**訓練微步驟（邏輯迴歸）**:
已廢止的四格步進條（預測 ŷ → 算 Cost J → 算梯度 → 更新參數）與訓練雙欄。
_Avoid_: 當作現行訓練畫面；逐步模式

**梯度演算板（邏輯迴歸）**:
已廢止的訓練期參數級 w／b／∂J 演示。
_Avoid_: 當作現行區塊

**樣本運算表（邏輯迴歸）**:
已廢止的訓練期前 5 筆 ŷ／ℓ 表。
_Avoid_: 當作現行區塊；與訓後 y／ŷ／predicted_class 表混稱

**學習階段（邏輯迴歸）**:
頁內用 horizontal radio 切換的教學段落（不用 tabs）。標籤為「線性邊界」「多項式與 λ」：前者大學錄取兩特徵、sigmoid 機率與直線決策邊界；後者微晶片資料上的多項式特徵映射與 λ 正則化。各階段自有決策槽狀態與訓練結果，切換不清掉另一側。
_Avoid_: Tab、分頁（指 st.tabs 實作）；階段名寫成完整「正則化邏輯迴歸」當 radio 主標籤；側欄獨立「正則化邏輯迴歸」頁；改成單變量／多變量；收成單一階段

**訓練前預測（邏輯迴歸）**:
解鎖訓練前的兩題關卡。階段「線性邊界」：sigmoid／機率意義，以及 Cost 依機率擬合、與 threshold 無關；階段「多項式與 λ」：為何多項式映射，以及 λ 變大時邊界／過擬合傾向。過關前可寫決策槽狀態，不可訓練（按鈕與訓練請求皆鎖）。Agent 不准代填選項。切分已是必寫槽，關卡不加第三題、也不換掉現有題。
_Avoid_: 僅一階段有關卡；硬鎖「必須先跟 Agent 說話才能組模型」；為切分加第三題或換掉 sigmoid／Cost／φ／λ 題

**訓練／測試切分（邏輯迴歸）**:
邏輯迴歸教學頁的決策槽，排在輸入資料之後；各學習階段各有一份。對該階段已鎖定的內建表切成訓練集與測試集。目前選擇為訓練集整數占比（1–99），測試為其餘；0 與 100 拒絕。學生在對話決定數字，資料 Agent 寫入，頁上無旋鈕、無數字框。進頁尚未選擇，沒寫不能訓。「組模型」預設寫入不含此槽，必須另寫。訓練吃訓練集；動畫期間與訓後都同時更新訓練 Cost 與測試 Cost，Cost 對迭代圖為兩條線。階段「多項式與 λ」的訓練 Cost 含課堂 λ/(2m)||w||²；測試 Cost 只算對數損失、不含 λ。正確率報訓練／測試兩筆。只留在該頁狀態，不寫檔。
_Avoid_: 資料切分（槽名；那是前處理側欄頁）；只報訓練集正確率；階段2 因資料少而省略切分；進頁預設 80／20 且當完成；把切分塞進組模型預設；測試 Cost 也加 λ 且用測試集 m

**分類 threshold**:
邏輯迴歸訓後才出現的滑桿。圖上決策邊界／contour 維持 ŷ=0.5；threshold 只改測試集混淆矩陣、其下的測試集 precision／recall／F1、測試集表上的 y／ŷ／predicted_class 與正確率。不是決策槽。
_Avoid_: 當決策槽；訓練中調整；決策邊界／contour 跟著 threshold 重畫；表改吃訓練集

**Sigmoid 函數視覺化**:
階段「線性邊界」的 expander，可在曲線上標 z。不佔主路徑，兩階段不都放。
_Avoid_: 當解鎖關卡；當決策槽；階段「多項式與 λ」再放一次

**多項式映射（邏輯迴歸）**:
階段「多項式與 λ」在特徵縮放之後、對已縮放的兩特徵做鎖定的 degree=6 映射。寫在輸入資料的選擇明細，不是決策槽，第一版不可改 degree。
_Avoid_: 當獨立決策槽；第一版開放 degree；攤 27 個 φ 當主路徑；先展開 27 維再縮放

**損失函數（邏輯迴歸）**:
決策槽之一，且為鎖定槽。種類固定對數損失，不可改成 MSE。框上的目前選擇只寫名稱：階段「線性邊界」為「對數損失」；階段「多項式與 λ」必須寫入 λ 才算完成，框上為「對數損失，λ=…」。「組模型」階段2 預設 λ=0.01。λ 不是獨立決策槽。`from_logits=True` 與 `BinaryCrossentropy` 不出現在框上。
_Avoid_: 頁上 λ 旋鈕；階段1 框上寫 λ；把 λ 當第七格；第一版開放 MAE 或改種類；框上寫 from_logits=True 或 BinaryCrossentropy

**線性層（邏輯迴歸）**:
決策槽之一，且為鎖定槽。目前選擇固定 `Dense(1, linear)`，網路輸出 z，不是機率。圖、表與測試集混淆矩陣所用的 ŷ 為 σ(z)。選擇明細拆成 z 一行與 ŷ 一行；階段2 另有 φ 行。
_Avoid_: Dense(1, sigmoid)；把第四格改名輸出層或 Sigmoid 層；訓練時先切成 0/1 再算 Cost

**特徵縮放（邏輯迴歸）**:
決策槽之一。目前選擇為課堂四種方法之一；Agent 組模型時預設寫入 Z分數正規化。兩學習階段都要做，沒有「不縮放」。同一選擇套用該階段訓練集的原始特徵；μ／max／σ 只從訓練集計算，測試列不參加 fit。階段「多項式與 λ」先縮放原始兩特徵，再做鎖定的多項式映射。散點、決策邊界與 contour 用原始特徵尺度，縮放後參數映回原尺度；不要把橫軸改成縮放後的 x。
_Avoid_: 階段2 省略縮放；兩階段永遠鎖 Z-score；先展開 27 維再縮放；橫軸改成縮放後的 x；用含測試列的全表來 fit；圖上用 Z 分數當座標

**測試集混淆矩陣**:
邏輯迴歸主教學欄裡、決策邊界或 contour 與 Cost 雙圖下方的第三張圖。只統計該階段測試集；列為實際 y=0／y=1，欄為預測類別 0／1，格子為計數。正下方帶出測試集 precision／recall／F1。開訓後與左圖、Cost 逐幀一起更新；分類 threshold 改這張矩陣與其下三數、表、正確率，不重畫邊界。不是測試集預測對照。
_Avoid_: 測試集機率對照、測試集預測對照；真陽性／假陽性當本頁格子名；把訓練點算進來；只在動畫播完才出現；拿本圖取代決策邊界或 Cost；決策邊界跟著 threshold 重畫

**測試集 precision／recall／F1**:
邏輯迴歸測試集混淆矩陣正下方的三個數字，順序即此。以 y=1 為正類，只由該張 2×2 計數算出；百分比、一位小數，與正確率同一寫法。無法計算時顯示 —；precision 或 recall 任一為 — 時 F1 也是 —。圖上不印公式。開訓後隨矩陣逐幀更新（當時依 ŷ=0.5 切）。資料 Agent 快照與模型下載程式碼都帶這三個數；快照在 — 時附原因（沒有預測為 y=1，或沒有實際 y=1）。不是決策槽，亦不報訓練集。
_Avoid_: 精確率、召回率、F1 值、F1 分數、以 y=0 為正類、macro 平均、訓練集的 precision／recall／F1、把公式印在混淆矩陣上、分母為 0 時填 0、寫成 0–1 小數、只在動畫播完才出現

**測試集機率對照**:
已廢止的舊第三圖：測試集實際 y vs 預測機率，加分類 threshold 水平虛線。由測試集混淆矩陣取代。
_Avoid_: 當作現行第三圖；與測試集預測對照混稱

**訓練動畫（邏輯迴歸）**:
開訓後主教學欄逐幀更新決策邊界或 contour、訓練 Cost、測試 Cost、測試集混淆矩陣與其下的測試集 precision／recall／F1 與一行狀態。Cost 對迭代圖畫兩條線（訓練／測試）。播完前決策槽列可見、框上目前選擇仍在，但不可點；訓練前預測與資料 Agent 欄凍結。播完恢復可點並留下該階段訓練結果。兩學習階段同一契約。
_Avoid_: 訓練微步驟；逐步模式；整頁跟著每一幀重畫；訓練中改決策槽狀態或送出對話；直接出結果而不播；動畫只播訓練 Cost、播完才出測試 Cost；圖上只留訓練一條線；測試集混淆矩陣或測試集 precision／recall／F1 等播完才出現

**訓練結果（邏輯迴歸）**:
該學習階段最近一次訓練留下的決策邊界或 contour、訓練 Cost、測試 Cost、學到的參數、測試集混淆矩陣與其下的測試集 precision／recall／F1，以及測試集的 y／ŷ／predicted_class 表。資料 Agent 快照含與動畫同一套的訓練／測試 Cost 曲線、四格計數、測試集 precision／recall／F1 與訓練／測試正確率。階段「多項式與 λ」的訓練 Cost 含 λ；測試 Cost 只算對數損失。左圖訓練點實心、測試點淡底不同色，測試點不當該散點主秀。改該階段決策槽狀態即作廢；另一階段不受影響。
_Avoid_: 與決策槽狀態混稱；只留一個 Cost 卻暗示已評估測試集；表改吃訓練集；測試點與訓練點同色同透明度；Agent 說看不到測試 Cost 或測試集 precision／recall／F1

**訓練請求（邏輯迴歸）**:
Agent 在決策槽狀態已齊且訓練前預測已過關時寫入的開訓訊號。系統走與「開始訓練」相同的訓練動畫；Agent 不得自行算完假裝已訓。
_Avoid_: 與決策槽狀態寫成同一件事；未過關或槽未齊仍寫入；沙盒 exec 訓練

**K-Means 分群**:
非監督式分群教學頁的正式名稱；指定群數 K，依距離把樣本分成 K 群。側欄與 Ward's Method 同屬「機器學習 · 非監督式」；僅使用內建範例資料（不提供 ready.csv 切換），沒有 ready 亦不鎖頁。本輪無關卡、無學習階段 radio；主按鈕為「開始分群演示」，以「下一步」走分群過程演進。不做類神經網路式實驗迴圈，亦不做 k-NN 式查詢點預測。
_Avoid_: KMeans（可作檔名／口語）、K 平均、以 ready.csv 當本頁資料來源；本輪上訓練前預測殼；本頁主按鈕「開始訓練」；本頁學習階段 radio；把預測過程演進／查詢點搬到本頁

**K（K-Means 分群）**:
要分成的群數。以 slider 設定；按「開始分群演示」當下鎖定該輪，演示進行中改 K 不套用，須重按開始才用新 K。範圍仍 2–8，預設 3。
_Avoid_: 本輪關卡解鎖後才露出 slider；演示中途改 K 立刻重算終局

**開始分群演示（K-Means 分群）**:
本頁主按鈕文案；按下後鎖定 K，並以固定種子隨機抽出該輪的初始群中心（非 k-means++、不多次重啟），但圖上此時仍只有未分群散點、不畫中心。本頁對應區塊標題為「分群過程演進」。重新按下可清空演進狀態後重跑（同一種子則同一組初始中心）。進頁尚未按開始時也不畫收斂著色；「下一步」要等開始後才可用。
_Avoid_: 本頁主按鈕、區塊標題或可見解鎖文案寫「開始訓練」／「開始預測演示」；按下即自動播放、放出中心或跑到收斂終局；進頁就畫 sklearn 終局；k-means++／n_init=10 當本頁主路徑

**分群過程演進（K-Means 分群）**:
按「開始分群演示」之後、以「下一步」推進的分群儀式（無自動播放、無逐步模式開關）。第一次「下一步」才放出初始群中心，點仍未分群（無著色、不畫連線）。之後每一輪兩拍：①全部點著色歸到最近中心 → ②中心移到該群平均，並淡留上一拍中心殘影（只一層；下一拍指派時清掉）。不畫點到中心的連線。指派不再改變、或滿 15 輪時停用「下一步」，並在狀態句說明已收斂或已達輪數上限；不默默跳到 sklearn 終局。空群的中心停在原地、不偷偷重放，狀態句標「有一群沒有點」。對照教學用真實群標籤整段演進都可開、預設關。
_Avoid_: 預測過程演進（本頁語意）；訓練過程演進；訓練微步驟（本頁）；time.sleep 自動播；逐步模式開關；一次點完一整輪 Lloyd 當唯一主秀；對 90 筆逐點指派或畫 90 條連線當主秀；空中心自動重放；以 Cost 曲線當主秀

**群內距離平方和（K-Means 分群）**:
當下已歸群的點到其所屬群中心的距離平方總和；只寫在分群過程演進的狀態句，不另畫曲線。尚未指派（含初始中心拍）不寫；①指派後開始寫，②搬家後更新數字。
_Avoid_: Cost、inertia（作頁上主文）、訓練損失曲線

**Ward's Method（階層分群）**:
非監督式階層分群教學頁的正式名稱；以 Ward 最小變異法合併，並以樹狀圖觀察過程。側欄與 K-Means 分群同屬「機器學習 · 非監督式」；僅使用內建範例資料（不提供 ready.csv 切換），沒有 ready 亦不鎖頁。第一版為開箱探索：樹狀圖為主，並可切成 k 群畫 2D 散點（與 K-Means／真實群對照開關共用視覺語言）；不做訓練前預測解鎖；學習階段／關卡之後再加。
_Avoid_: Ward's method 當唯一標題、階層聚類當頁標題主文；以 ready.csv 當本頁資料來源；第一版就上訓練前預測殼；第一版只有樹狀圖、完全不能切群著色

**內建範例資料（非監督式分群）**:
K-Means 分群與 Ward's Method 兩頁第一版共用的主資料：兩特徵、可畫 2D、尺度設計為相近；另含教學用真實群標籤（**3** 個潛在群）。UI 固定使用這兩特徵（不提供欄位下拉）；第一版不露標準化開關。K-Means 的 K 與 Ward 切群數預設皆為 **3**。有明確教學需求再開對照／陷阱資料（類似 K-近鄰尺度對照）再講標準化，不預設為每頁各一份。
_Avoid_: ready.csv 作為本頁資料來源；第一版就強制兩頁分叉資料；高維主路徑；第一版欄位 multiselect；第一版標準化 checkbox；第一版預設故意設錯 K／切群數

**教學用真實群標籤**:
內建分群範例裡供著色／對照「潛在結構」的群欄；演算法只吃數值特徵，不把此欄當訓練輸入或監督答案。圖上預設不並排真實群；以開關才對照（群編號各自獨立，不可假設同號同色）。K-Means 分群過程演進整段都可開此開關；尚未指派時演算法側不是收斂後的著色。
_Avoid_: 目標欄、標籤欄（未加「教學用／真實群」易與監督式 y 混淆）、y；一進頁就強制並排真實群主視覺；等到收斂才准對照；用同一色表把演算法群號與真實群號直接疊加當成對齊

**圖表探索**:
前處理側欄頁；以選對圖種關卡練習視覺化，資料來源固定為 Working 工作資料。側欄仍在特徵縮放之後、資料切分之前。沒有 Working 時空狀態，指向欄位與資料整合；有缺失或未編碼欄仍可畫。
_Avoid_: Ready；本頁切換 Original；以 train／val／test 當本頁資料來源；沒清理完就鎖頁；為邊清邊看而前移側欄

**PCA 主成分分析**:
降維分析側欄頁；以 Working 工作資料做主成分分析與條件檢查。沒有 Working 時空狀態，指向欄位與資料整合；欄位或樣本不足時警告並停算，不是 Ready 鎖。
_Avoid_: 鎖 ready.csv；以切分產物當本頁來源；把分群教學頁掛回 Working

**資料切分**:
前處理側欄頁的正式名稱；把 Working 工作資料拆成訓練／驗證／測試三份並寫出檔案。側欄在圖表探索之後（圖表仍只讀 Working，不讀切分產物）。預設比例為訓練 60%／驗證 20%／測試 20%。有類別目標欄時預設做分層切分（鐵達尼為 Survived），使各份類別比例接近整體。與決策樹頁內的「訓練／驗證切分」不是同一概念。寫回 Working 會作廢既有切分產物。「清除回雙表起點」亦一併清除切分產物。沒有 Working 時空狀態；不因缺失或未編碼而鎖頁。
_Avoid_: 訓練／驗證切分（指決策樹頁內靜默 holdout）、資料集切分、train-test split（作頁標題）；Ready 切分；改 Working 或回雙表後仍沿用舊 train／val／test；暗示圖表探索讀的是 train／val／test；預設暗示為 70／15／15

**分層切分**:
資料切分時依目標類別比例抽樣，使訓練／驗證／測試中各類占比接近 Working 整體。本專案前處理線於資料切分頁提供；有目標欄時預設開啟。
_Avoid_: 分層抽樣（可作口語）、stratify（作頁標題主文）；無目標欄時仍強制分層

**雙表起點**:
課堂整合線的起始狀態與協作區入口狀態：無 working／切分產物。進「欄位與資料整合」時從內建雙表教材寫出雙表工作副本。「清除回雙表起點」刪除 Working／Original／切分／副本與遺留 ready.csv 後回到此狀態。不提供單表 CSV 上傳進 Original／Working。
_Avoid_: 僅刪 working 卻留下 train／val／test 或雙表工作副本仍稱為回到起點；以「資料上傳與預覽」或上傳 CSV 作為協作線起點；側欄再設僅名為「資料整合」的獨立頁才算起點完成；把舊 ready.csv 當成起點還沒收乾淨的條件；與專案展示的清除回起點確認混稱

**內建雙表教材**:
課堂只讀的乘客表／航程表 CSV（`built-in-data/integration/`）。不得覆寫。進雙表起點頁時複製為雙表工作副本。
_Avoid_: Original、Working、雙表工作副本

**雙表工作副本**:
合併前 Agent 可改的兩份檔（`workspace/integration/passengers.csv`、`voyage.csv`）。雙表起點進頁即確保兩份存在。合併前只准對齊航程表鍵名，不得另存新檔。不是 Original，也不是 Working。畫面讀副本；「重新讀取雙表」重讀副本；「恢復內建雙表」刪副本後由進頁再次從教材寫出。資料整合寫入成功後刪除副本。
_Avoid_: working.csv、original.csv；直接改內建雙表教材；把副本叫 Original 或 Working；合併前對副本做鍵名以外的清理；合併前按鈕寫「重新讀取工作資料」或「回到原始資料」；靠 Agent 自行建立目錄或自創檔名

**欄位與資料整合（雙模態）**:
「AI 協作資料整理」側欄第一項；無 working.csv 時並排乘客表與航程表以便找合併鍵，並在本頁請 Agent 對齊鍵後做資料整合（主教學欄含訓練前預測（資料整合），控制為「重新讀取雙表」「恢復內建雙表」）；有 working.csv 時呈現單表欄位診斷（控制為「重新讀取工作資料」「回到原始資料」與「清除回雙表起點」）。無 Working 時的空狀態導引一律指本頁。
_Avoid_: 欄位與資料概覽（舊側欄名）；側欄另設僅名為「資料整合」的頁；用 phase 檔判斷整合前後（以有無 working 為準）；側欄第一項「資料上傳與預覽」；以「套用合併並寫入工作資料」按鈕寫入；雙表起點頁用「目前還沒有 Working」當空狀態講義（雙表本身即起點；Working 等到資料整合寫入再出現）

**資料整合**:
在「欄位與資料整合」上由 Agent 執行的合併動作，不是側欄頁名。學生先請 Agent 對齊航程表鍵名（寫入雙表工作副本，可在關卡前；合併前不准做鍵名以外的清理），通過訓練前預測（資料整合）後再請 Agent 以 left 或 inner 合併，寫入 Original 原始資料與 Working 工作資料。未對齊鍵名不得合併。左表固定為乘客表、右表固定為航程表（不可對調）。課堂主線建議 left。此動作是 Agent 唯一被允許寫入 Original 的時刻。
_Avoid_: 把側欄頁名縮成「資料整合」；合併（作側欄頁標題）、join 頁；UI 對調左右表；以上傳 CSV 建立 Original／Working；「套用合併並寫入工作資料」按鈕

**訓練前預測（資料整合）**:
欄位與資料整合在雙表起點的兩題關卡；答對後 Agent 才被允許執行資料整合寫入 Original／Working。不鎖對齊鍵名。題意仍是先對齊鍵名、以及 left 會保留全部乘客；解鎖後 inner 與 left 皆可寫入。
_Avoid_: 解鎖套用（舊按鈕語意）；關卡通過前禁止改鍵名；關卡通過後仍禁止 inner 寫入

**Original 原始資料**:
資料整合寫入時與 Working 同批建立的重置來源；不直接修改。合併完成前不存在。
_Avoid_: 上傳後保留（舊說法）；暗示仍有單表上傳頁建立 Original；把尚未合併的單表或雙表工作副本當成 Original

**Working 工作資料**:
雙表整理線根目錄的可變工作副本；資料整合時與 Original 同批寫入，之後清理、圖表探索、PCA、資料切分都讀這份，寫回即作廢切分產物並刪掉遺留 ready.csv。總覽可下載目前這份。與 Challenge 工作資料不是同一份。
_Avoid_: Ready；把 Challenge 工作資料叫 Working；圖表或切分改吃 Original 或 train／val／test；為下載而留 Ready 頁

**Ready 分析就緒資料**:
已廢止。舊協作線把 Working 凍結成獨立分析表，圖表探索、PCA、資料切分曾鎖該檔。上述頁改讀 Working 工作資料。寫回 Working 與清除回雙表起點時刪掉磁碟遺留檔，不當分析來源。
_Avoid_: 當作現行生命週期；分析頁鎖 ready.csv；把遺留檔當分析來源

**建立 Ready 分析就緒資料**:
已廢止的舊側欄頁：把 Working 凍結成 Ready。分析頁不經此頁；側欄只刪此頁、不前移圖表探索。
_Avoid_: 當作現行側欄頁；分析前必須先凍結；為下載而留此頁

**乘客表**:
鐵達尼整合教材的左表（畫面名稱）；對應內建乘客側欄位，含身分與 Survived 等。
_Avoid_: passengers 表（可作檔名／副標）、左表（未指明乘客時）

**航程表**:
鐵達尼整合教材的右表（畫面名稱）；對應內建航程側欄位，含 Ticket／Fare／Cabin／Embarked 等；鍵欄刻意為 `passenger_id`。
_Avoid_: voyage 表（可作檔名／副標）、右表（未指明航程時）

**資料轉換**:
前處理側欄頁的正式名稱；把同一語意的不同寫法收斂成約定標準值（如性別混寫）。頁內以欄位輔助資訊與同義提示協助選欄；寫回以 Agent 更新 working 為主；不是編碼、不是特徵工程。
_Avoid_: 編碼、特徵工程、正規化（指特徵縮放）；本頁 UI 主按鈕套用 mapping（第一版）；把同義提示當硬性必轉清單
