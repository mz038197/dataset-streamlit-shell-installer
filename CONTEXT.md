# Dataset Streamlit Shell

教學用 Streamlit 資料實驗室殼層：自雙表起點經整合與整理建立 Ready，並以內建範例走通監督式與非監督式演算法頁；另含 AI 新創工作坊的專案展示軌道。

## Language

**AI新創工作坊**:
側欄導覽分段名稱；與「降維分析」同層。底下目前僅「專案展示」一頁。
_Avoid_: Startup Challenge（作側欄分段名）、挑戰區、展示區

**專案展示**:
AI新創工作坊下的側欄頁正式名稱；極簡上台白板（白板三塊），不是重做整條 ML 教學實驗室。發佈給學生時可為空殼（② 留待學生／Agent 補齊）。頁內主標為「專案展示」，副標為「AI Startup Challenge｜成果展示」。
_Avoid_: Startup Challenge 展示頁（舊草案頁名）、展示空頁（備課狀態口語）、以「成果展示」作側欄名、上台白板（口語）

**白板三塊**:
專案展示頁主教學欄的三個固定區塊：①我們在解決什麼？②我們做出來的結果？③我們不能亂承諾什麼？第一版以 Agent 改允許範圍內的 UI／腳本把空殼補成可展示內容（AI coding），不以表單暫存當成果真相。
_Avoid_: 三欄、三步驟教學、ML 流水線階段；以 text_input／session_state 當上台成果的唯一載體

**挑戰公司**:
專案展示的五選一委託情境識別：`edupulse`／`vitalrisk`／`airsense`／`churnlab`／`flowcast`；決定 Challenge 起點資料、資料說明書與倫理加碼。
_Avoid_: 把公司 id 與側欄頁名混稱；混用其他公司的 CSV

**Challenge 起點資料**:
各挑戰公司內建的只讀起點 CSV（`workspace/challenge/{company}.csv`）。可髒、供診斷與複製清理；不得覆寫此檔。與雙表線的 Original 原始資料不是同一概念。
_Avoid_: Original 原始資料（未加 Challenge 前綴）、起點檔（未鎖定挑戰語境時）

**Challenge 工作資料**:
學生清理後的工作副本（`workspace/challenge/working.csv`）。訓練／預測優先使用；與雙表線根目錄的 Working 工作資料不是同一份。第一版不強制走 Ready／資料切分產物，亦不強制 cleaning_log。更換挑戰公司時清除或封存此工作副本，但不還原已改過的專案展示 UI 程式。
_Avoid_: Working、working.csv（未指明 challenge 軌道）；暗示挑戰線要先建立 Ready；假設換公司會自動還原白板程式內容

**Challenge 資料說明書**:
各挑戰公司給學生的欄位與委託說明（`workspace/challenge/{company}_資料說明書.md`）。解釋欄位與目標前應先讀；教師用缺陷說明不進學生專案。
_Avoid_: 教師用缺陷說明、劇透缺陷清單

**Challenge host context**:
專案展示頁專用的 Agent `host_context`：定角色、挑戰軌道檔案邊界、白板三塊完成樣貌，並串上當前挑戰公司的倫理／資料加碼片段；不叠加雙表線的 `dataset_base_context`（含 NN fragment）。加碼只強化必講紅線與檢查方向，不剧透教師缺陷清單。
_Avoid_: 挑戰頁直接叠 Titanic／Ready／NN 的 dataset_base_context；靠「衝突以挑戰為準」口頭覆寫卻仍灌入根目錄 working 規則；在 host 列出老師埋的缺陷清單

**Challenge Agent session**:
專案展示頁專用的對話 session；與雙表整理線的 Agent session 分開。進頁或更換挑戰公司時，以 Challenge host context 重建 Agent。
_Avoid_: 與全站整理頁共用同一條 session 卻不換 host；只靠每輪 snapshot 提醒卻不重建

**Challenge 允許改動範圍**:
專案展示軌道上 Agent／學生預設可改：`ui/startup_challenge_ui.py`、`workspace/challenge/*`（含 Challenge 工作資料）、必要時 `scripts/`。不改其他 ML 教學頁；頁入口薄包裝與側欄導覽由老師預放。
_Avoid_: 為挑戰去改邏輯回歸等教學頁；把挑戰成果寫進根目錄 working／ready

**Challenge 上台 Gate**:
人審／自評的最小可上台規準（白板三塊可講、有處理過資料並用 Challenge 工作資料訓練或能說明等價流程、含該公司必講紅線、分得清 Agent 與人類決策）。第一版不做機器硬檢查；每組自選獨特畫面為軟規則。頁上可放簡短 checklist 文案，僅提示用。
_Avoid_: 第一版就做 Gate 自動驗收；把獨特畫面做成硬檢查

**主教學欄**:
內容區左側的教學主體區；承載頁標題、學習階段與演算法互動。與 Streamlit 左側導覽側欄不同。
_Avoid_: 中欄、中間欄位（未鎖定全站殼時）、主欄（易與線性回歸頁內「模型」欄混淆）

**資料 Agent 欄**:
內容區右側的資料 Agent 協作區；與主教學欄並排，寬度可調時指此欄。
_Avoid_: 右欄、右邊欄位（未鎖定全站殼時）、側欄（本專案「側欄」專指導覽）

**內容區雙欄殼**:
全站內容區的版面契約：主教學欄與資料 Agent 欄並排；主教學欄在視窗高度內獨立縱向捲動，資料 Agent 欄釘在視窗內；欄內僅訊息列表自捲，`chat_input` 釘在欄底；附圖不在第一版 Agent 欄。資料 Agent 欄寬可拖曳調整並全站共用同一記住寬度。不含 Streamlit 左側導覽側欄，亦不涵蓋線性回歸頁內的目前查看焦點單欄或訓練雙欄。
_Avoid_: 三欄殼（易與 waku 左 nav 或舊回歸三欄混淆）；側欄雙欄；把導覽側欄算進本殼；Agent 欄整欄第二條捲軸；輸入浮在訊息流中間

**側欄品牌列**:
導覽側欄頂部的品牌列；左為 logo、右為固定字樣 `VansCoding.AI`。視覺對齊 classroom 的品牌列。不是頂部 Deploy chrome，也不屬內容區雙欄殼；側欄收合時頂部不出現字樣。
_Avoid_: header（未加「側欄」易與頂部 chrome 混淆）、頂欄、導覽列、把 classroom 的「凡思課堂安裝」當本殼字樣

**K-近鄰分類**:
監督式分類教學頁的正式名稱；以距離找 k 個鄰居、多數決決定類別的實例型（懶惰）學習。側欄插在線性支持向量機與決策樹與集成之間；僅使用內建範例資料（不提供 ready.csv 切換）。採訓練前預測殼（兩題解鎖＋Agent 提示），解鎖後主按鈕為「開始預測演示」並播放預測過程演進；不做類神經網路式實驗迴圈。
_Avoid_: KNN 分類（可作副標／口語）、K 最近鄰、k-NN、KNN 回歸；本頁主按鈕「開始訓練」

**決策樹與集成**:
監督式分類教學頁的正式名稱；由單顆決策樹走到隨機森林再到 XGBoost，帶出 Bagging 與 Boosting 差異。側欄單一頁（併入原獨立 XGBoost 頁）；頁內三個學習階段；僅使用內建範例資料。
_Avoid_: 決策樹概念（舊側欄名）、側欄分「決策樹概念」「XGBoost」兩頁、樹模型（作頁標題）

**學習階段（決策樹與集成）**:
頁內用 horizontal radio 切換的教學段落（不用 tabs）。標籤依序為「單顆決策樹」「隨機森林（Bagging）」「XGBoost（Boosting）」。Bagging vs Boosting 對照以階段3開頭的對照區塊為主，並以訓練前預測題強制區分「平行多數決 vs 序列糾錯」；不另開第四階段。
_Avoid_: Tab、分頁（指 st.tabs 實作）；階段標籤只寫演算法名而不標 Bagging／Boosting；另開第四段專講 Bagging vs Boosting；只靠走過兩段自行領悟、無對照文／無關卡

**Bagging（決策樹與集成）**:
多棵樹大致平行訓練、再彙總（如多數決）的集成策略；本頁以隨機森林為教學承載。
_Avoid_: 把 Bagging 當獨立側欄頁；第一版深講 bootstrap 抽樣公式細節當主菜

**Boosting（決策樹與集成）**:
多棵弱學習器序列訓練、後者針對前者錯誤調整的集成策略；本頁以 XGBoost 為教學承載。
_Avoid_: 把 Boosting 當獨立側欄頁；第一版深講二階導數／自訂 loss 當主菜

**訓練前預測（決策樹與集成）**:
解鎖主按鈕前的概念關卡；三個學習階段各兩題。階段1：分裂／不純度；階段2：平行訓練與多數決（Bagging）；階段3：與 Bagging 對照的序列糾錯（Boosting）。
_Avoid_: 僅階段3有關卡；硬鎖「必須先跟 Agent 說話才能訓練」

**內建範例資料（決策樹與集成）**:
階段1 使用小筆數玩具 CSV（可手算熵／資訊增益）；階段2 與階段3 共用同一份較完整的分類 CSV（現行為心臟病資料），使 Bagging vs Boosting 差異不歸因於換資料。皆不提供 ready.csv 切換。
_Avoid_: ready.csv 作為本頁資料來源；三階段各用不同主資料；全程只用 10 筆玩具跑隨機森林／XGBoost

**隨機森林（決策樹與集成）**:
階段2 的 Bagging 承載；解鎖後以 `n_estimators` 為主旋鈕，並以單顆決策樹 vs 森林的驗證準確率並排對照。特徵隨機／bootstrap 細節可降級補充，不當第一版主菜。
_Avoid_: 第一版主路徑露出 `max_features` 當第二主旋鈕；以投票動畫取代準確率對照當唯一主秀

**XGBoost（決策樹與集成）**:
階段3 的 Boosting 承載；開頭為 Bagging vs Boosting 對照區塊，解鎖後以 `learning_rate` 為單一主旋鈕，搭配訓練／驗證準確率。`n_estimators` 固定合理預設。one-hot 與訓練／驗證切分可靜默沿用。舊獨立頁的雙超參數掃描圖與 early stopping 敘事不做第一版主路徑。
_Avoid_: 階段3 主路徑保留 n_estimators＋learning_rate 雙掃描；以 early stopping 當第一版主菜；獨立側欄「XGBoost」頁；階段3 主旋鈕再用 `n_estimators` 與階段2 撞名

**訓練／驗證切分（決策樹與集成）**:
階段2 與階段3 共用同一 80/20 切分與同一 random_state。階段3 結果區顯示同切分下的隨機森林驗證準確率作為 Bagging 對照基線。屬該教學頁內部 holdout，不寫出前處理的 train／val／test 檔，亦非「資料切分」頁。
_Avoid_: 兩階段各切各的當預設；用不同資料切分比較 Bagging／Boosting 分數；與「資料切分」混稱；暗示本頁會改寫 workspace 的 train.csv

**單顆決策樹（決策樹與集成）**:
階段1 承載；沿用現有玩具資料上的熵／資訊增益表、分裂準則與淺層 max_depth、決策樹圖與訓練準確率，並加上該階段兩題訓練前預測。不做本階段驗證切分主路徑。
_Avoid_: 階段1 改成 2D 決策邊界主秀；為集成線先砍掉準則二選一；階段1 主秀改跑心臟病資料

**學習階段（K-近鄰分類）**:
頁內用 horizontal radio 切換的教學段落（不用 tabs）。階段1 標籤「鄰居與投票」：建立找鄰居＋多數決；階段2 標籤「選擇 k」：以 k 為主角（可帶尺度相關觀察）。各階段各自兩題訓練前預測。
_Avoid_: Tab、分頁（指 st.tabs 實作）；階段1 標籤出現「k／超參數」當主菜

**k（K-近鄰分類）**:
要查詢的最近鄰居個數。階段1 固定且不在 UI 露出（固定為奇數預設）；階段2 以奇數步進的 slider 調整。第一版距離固定歐氏、投票為多數決（uniform）；不露 weights／metric 進階項。
_Avoid_: 階段1 的 k slider（含 disabled 顯示）；第一版強調平票處理流程

**內建範例資料（K-近鄰分類）**:
兩特徵、二元分類的教學用 CSV。階段1／2 以可畫 2D 決策邊界的資料為主；階段2 另具備刻意尺度失衡的對照，供關閉標準化時觀察距離被某一軸主導。
_Avoid_: ready.csv 作為本頁資料來源；第一版 multiclass／高維主路徑

**訓練前預測（K-近鄰分類）**:
解鎖主按鈕前的概念關卡。階段1：實例型（無 w）與多數決；階段2：k 過大趨向過度平滑／多數類，以及未標準化時大尺度特徵主導距離。解鎖後的主按鈕為「開始預測演示」（非「開始訓練」）。
_Avoid_: 類神經網路式實驗迴圈；硬鎖「必須先跟 Agent 說話才能訓練」；本頁主按鈕沿用「開始訓練」

**查詢點（K-近鄰分類）**:
用來示範預測的單一座標點；畫出其 k 個最近訓練鄰居（可連線），讓「鄰居與投票」可見。以圖上 click 指定；本頁因此採用可點選的互動圖（Plotly），不沿用其他 ML 頁的 matplotlib＋st.pyplot 主路徑。
_Avoid_: 第一版以 confusion matrix／classification report 當主視覺；強制全專案改 Plotly

**開始預測演示（K-近鄰分類）**:
本頁解鎖後的主按鈕文案；按下後 fit 鄰居池、畫決策邊界，並啟動預測過程演進。本頁對應區塊標題為「預測演示」；學生可見的解鎖／提示文案一併避開「開始訓練」。其他監督式頁仍用「開始訓練」。
_Avoid_: 本頁主按鈕、區塊標題或可見解鎖文案寫「開始訓練」／「##### 訓練」；為對齊本頁而改其他 ML 頁按鈕

**預測過程演進（K-近鄰分類）**:
按「開始預測演示」後播放的預測儀式：訓練集固定留作鄰居池；決策邊界全程顯示。以內建 **3 筆**示範查詢點（新資料、非訓練列）依序進來。對每一點：鄰居依距離一筆一筆進場並累加票數（查詢點改預測類別色＋短 caption 含票數）→ 多數決標類別 → 再下一點。已標示的新點以預測色留在圖上；**鄰居連線只顯示當下正在預測的那一筆**。播完後仍可 click 加演，**示範點與後續 click 點皆累積保留**；重新按「開始預測演示」可清空重跑。兩學習階段皆有。對齊懶惰學習的預測，不是學權重／降 Cost 的訓練迴圈。
_Avoid_: 訓練過程演進（本頁語意）；偽 epoch／Cost 曲線；對訓練集逐筆預測／leave-one-out 當主秀；第一版另備 holdout CSV（示範查詢點用固定座標即可）；單點內 k 條連線一次出現當唯一節奏；自動串超過 3 筆（第一版）；已預測新點的連線全部留在圖上；演進時隱藏決策邊界；click 後清掉示範點（第一版）

**卷積神經網路（CNN）**:
影像辨識觀念教學頁的正式名稱；以小視窗（kernel）在圖上滑動找局部特徵，再經活化與縮小後分類。敘事以指定觀念主軸影片為骨幹，開頭保留「圖片＝矩陣」前置；互動示範用來驗證觀念，不是實驗迴圈主體。
_Avoid_: CNN 介紹（可作檔名／口語）、Convolutional Neural Network 當頁標題主文、把 inductive bias 專名當第一版主菜

**學習階段（卷積神經網路）**:
頁內用 horizontal radio 切換的教學段落（不用 tabs）。六段依序為：圖片與矩陣；為什麼需要 CNN；卷積與特徵圖；ReLU 與 Pooling；CNN 整條流程；動手做 CNN。
_Avoid_: Tab、分頁（指 st.tabs 實作）；把六段併回「局部只改兩 tab」的舊結構

**觀念主軸影片（卷積神經網路）**:
DataMListic《CNNs - Explained》（video_id `YGILT182T6w`）。階段1不嵌入；階段2～6嵌入同一支片並附該段建議時間戳；正文為繁中整理，影片為對照。
_Avoid_: 影片僅備課不進學生 UI；硬鎖「必須看完整支才能往下」；第一版灌譯 inductive bias 專名

**訓練前預測（卷積神經網路）**:
階段6解鎖「開始訓練」前的兩題概念關卡：kernel／卷積在做什麼，以及 pooling 留下什麼。
_Avoid_: 階段6直接訓練無關卡；把「為什麼不用 Dense」或 ReLU 當解鎖硬門檻；硬鎖「必須先跟 Agent 說話才能訓練」

**史話補充（卷積神經網路）**:
Hubel & Wiesel 與 LeNet-5 等歷史／生物啟發內容；降級為可展開補充，不佔「為什麼需要 CNN」主線。
_Avoid_: 與影片動機並列當階段2正文主軸

**教學流程圖**:
教學頁上把演算法路徑畫成可導覽、可在作動時點亮的節點流程（非側欄頁）。拓撲與節點文案依各頁定義；可複用於多頁，但各頁節點不必相同。
_Avoid_: 整條流程（CNN 舊口語可保留於該頁）；把流程圖當唯一學習階段取代 radio；暗示為可編輯的工作流引擎

**教學流程圖（線性回歸）**:
線性回歸頁的粗骨架：頂部自研 SVG 拓撲條「輸入資料 → 回歸模型 → 輸出呈現」。節點不可點；另以「目前查看」切換焦點單欄（預設回歸模型）。模型節點：預測公式常駐（單變量 \(Y=WX+B\)；多變量一開始即展開 \(Y=W_1X_1+\cdots+B\)，本頁房價為四特徵），Cost／GD 公式收在 expander。兩學習階段共用此拓撲；各階段訓練結果各自記住，切換學習階段不清掉另一側 artifact。
_Avoid_: 下方三欄並排掛內容（已廢）；獨立長段「模型公式」與流程圖重複敘事；節點名「模型公式」；可點節點展開；輸出僅顯示係數；未訓練多變量只用向量式 \(W\cdot X+B\)；Mermaid／graphviz 當本頁流程圖；切階段強制清空兩側結果；訓練中每步重寫整條主公式

**目前查看（線性回歸）**:
教學流程圖下方、平時用以切換焦點單欄的控制（horizontal radio／segmented）。標籤與三節點相同：輸入資料／回歸模型／輸出呈現；預設為回歸模型。不可點 SVG 節點代替此控制。
_Avoid_: 三欄同時展開；以可點 SVG 節點當唯一導覽

**訓練微步驟（線性回歸）**:
按「開始訓練」後、疊在粗骨架下的迷你步進條；每一（含動畫抽樣後的）gradient descent iteration 依序走完四步：預測 ŷ → 算 Cost J → 算梯度 → 更新參數，再進下一 iteration。訓練時版面改為左梯度演算板（下接樣本運算表，僅前兩微步）、右回歸線／Cost；播完自動切到「輸出呈現」焦點。預設自動播放；可開「逐步模式」按「下一步」前進。
_Avoid_: 把微步驟升格成頂部第四、第五粗節點；自動播放時略過微步驟只跳 iteration；播完留在訓練雙欄不切輸出

**梯度演算板（線性回歸）**:
訓練微步驟期間左側上方的參數級數值演示：單變量用直式板，w 與 b 同等完整展示（含 ∂J／∂·、Δ、更新後）；多變量用表格式（一列一特徵），b 在表尾一行完整。與右側圖同步。不承載逐筆 ŷ／y／誤差列（見樣本運算表）。
_Avoid_: 多變量四特徵卡片並排；只旁附 W／B／J 字幕而無梯度／Δw；多變量表省略 b；把逐筆 ŷ／y／error 表併進本板標題

**樣本運算表（線性回歸）**:
訓練微步驟期間、梯度演算板下方的逐筆數值表；僅在「預測 ŷ」「算 Cost J」兩微步顯示（算梯度／更新參數時不顯示）。固定訓練資料前 5 筆；欄名用教學符號。兩階段表上的輸入 x 皆為 Z-score 後、與當前 w 相乘的同一組（須註記已縮放）。預測步：各輸入 x（單變量一欄；多變量各特徵一欄）與 ŷ、y；Cost 步再加 error、error²，並以一行顯示整批純量 Cost J（非逐列一個 J）。
_Avoid_: 併入「梯度演算板」同一標題；四微步都掛表；逐列欄位叫 J；以輸出區「預測與誤差」表取代本表時點；預測步就攤開 error；表上放未縮放原始特徵；未註記已縮放

**線性回歸**:
監督式回歸教學頁的正式名稱；側欄單一頁，內含單變量與多變量兩個學習階段。各階段僅用對應內建範例資料（固定欄位，不提供欄位下拉）；採訓練前預測殼；頁面主骨架為教學流程圖（線性回歸）＋目前查看焦點單欄，訓練時切訓練微步驟與梯度演算板；不做本頁保存模型 JSON／手動預測。
_Avoid_: 側欄分「單變量線性回歸」「多變量線性回歸」兩頁；把欄位選擇當成主路徑；以長捲三欄並排取代焦點單欄

**學習階段（線性回歸）**:
頁內用 horizontal radio 切換的教學段落（不用 tabs）。標籤為「單變量」「多變量」：前者餐廳獲利一特徵一線；後者房價多特徵。兩階段訓練特徵皆經 Z-score；共用同一教學流程圖拓撲與訓練微步驟。單變量右圖散點仍用原始特徵尺度，回歸線以縮放後參數正確映射回原尺度；樣本運算表則顯示 Z-score 後的 x。
_Avoid_: Tab、分頁（指 st.tabs 實作）；階段名寫成完整「單變量線性回歸／多變量線性回歸」當 radio 主標籤；兩階段各做不同節點拓撲；僅單變量有梯度演算板；單變量訓練用原始 x 卻在表上標已縮放；單變量圖用「原始 x × w」硬畫以致線歪

**邏輯迴歸**:
監督式二元分類教學頁的正式名稱；側欄單一頁，內含線性邊界與多項式與 λ 兩個學習階段。標籤語意為 `0`／`1`，散點圖例為 `y=0`／`y=1`（非 ±1）；各階段僅用對應內建範例資料；採訓練前預測殼；頁面主骨架為教學流程圖（邏輯迴歸）＋目前查看焦點單欄，訓練時切訓練微步驟與梯度演算板；不做本頁保存模型 JSON／手動預測，亦無頁首說明 `st.info`。
_Avoid_: Logistic Regression 當頁標題主文；側欄分「邏輯迴歸」「正則化邏輯迴歸」兩頁；圖例寫 y=-1／y=+1；本頁保存／手動預測區塊；頁首長文 info 複述訓練流程；以長捲取代焦點單欄；節點名沿用「回歸模型」

**教學流程圖（邏輯迴歸）**:
邏輯迴歸頁的粗骨架：頂部自研 SVG 拓撲條「輸入資料 → 分類模型 → 輸出呈現」。節點不可點；另以「目前查看」切換焦點單欄（預設分類模型）。模型節點：預測公式常駐，拆成 z 一行與 ŷ 一行（階段1：\(z=w\cdot x+b\)、\(ŷ=\sigma(z)\)；階段2：現有 φ 行 + \(z=w\cdot\phi(x)+b\) + \(ŷ=\sigma(z)\)）。ŷ 為 sigmoid 機率，不是類別。Cost／GD 公式收在 expander；階段1 另有 Sigmoid 函數視覺化 expander。訓練中主公式保持符號式，不每步重寫整條。兩學習階段共用此拓撲；各階段訓練結果各自記住，切換學習階段不清掉另一側 artifact。輸出呈現含訓練後分類 threshold 滑桿；圖上決策邊界／contour 維持 ŷ=0.5，threshold 只改表上的 y／ŷ／predicted_class 與正確率。
_Avoid_: 節點名「回歸模型」；下方三欄並排掛內容；可點節點展開；Mermaid／graphviz 當本頁流程圖；切階段強制清空兩側結果；把分類模型理解成訓練時先切成 0/1 再算 Cost；輸出圖跟著 threshold 重畫；只寫一個長分式、不拆 z；訓練中每步重寫整條主公式

**目前查看（邏輯迴歸）**:
教學流程圖下方、平時用以切換焦點單欄的控制（horizontal radio／segmented）。標籤與三節點相同：輸入資料／分類模型／輸出呈現；預設為分類模型。不可點 SVG 節點代替此控制。
_Avoid_: 三欄同時展開；以可點 SVG 節點當唯一導覽；標籤寫「回歸模型」

**訓練微步驟（邏輯迴歸）**:
按「開始訓練」後、疊在粗骨架下的迷你步進條；每一（含動畫抽樣後的）gradient descent iteration 依序走完四步：預測 ŷ → 算 Cost J → 算梯度 → 更新參數。ŷ=σ(z)∈(0,1)，不是 0/1 類別。訓練時版面改為左梯度演算板（下接樣本運算表，僅前兩微步）、右決策邊界或 contour／Cost；播完自動切到「輸出呈現」焦點。預設自動播放；可開「逐步模式」。微步驟期間不出現分類 threshold。
_Avoid_: 把微步驟升格成頂部第四、第五粗節點；加第五步「先算 z」；自動播放時略過微步驟只跳 iteration；播完留在訓練雙欄不切輸出；訓練過程調整 threshold

**梯度演算板（邏輯迴歸）**:
訓練微步驟期間左側上方的參數級數值演示。階段「線性邊界」用表格式（一列一特徵，兩科＋表尾 b），與線性回歸多變量同一規則。階段「多項式與 λ」主路徑完整秀 b、‖w‖² 與 λ，並秀正則化後的 ∂J／∂w（對 b 不加 λ）；全部 w 仍收 expander。與右側圖同步。不承載逐筆 ŷ／y／損失列。
_Avoid_: 階段2 主路徑 27 列閃爍；對 b 加 λ；把逐筆 ŷ／y 表併進本板標題；階段2 主路徑只靠 ‖w‖²、梯度式完全不露面

**樣本運算表（邏輯迴歸）**:
訓練微步驟期間、梯度演算板下方的逐筆數值表；僅在「預測 ŷ」「算 Cost J」兩微步顯示。固定訓練資料前 5 筆。階段「線性邊界」表上 x 為 Z-score 後、與當前 w 相乘的兩特徵（須註記已縮放）；預測步為各 x、ŷ、y；Cost 步再加 `ℓ`（log-loss 逐筆項；整批 J 是平均，階段2 另含 λ 項）與一行整批純量 Cost J。階段「多項式與 λ」不攤 27 個 φ，只留 ŷ、y（＋Cost 步 `ℓ`），並註明 ŷ=σ(w·φ(x)+b)。不出現 error／error²。
_Avoid_: 併入「梯度演算板」同一標題；四微步都掛表；逐列欄位叫 J；欄名「逐筆損失」當主欄；照抄 error／error²；階段2 攤全部 φ；階段1 表上放未縮放原始分數；未註記已縮放

**學習階段（邏輯迴歸）**:
頁內用 horizontal radio 切換的教學段落（不用 tabs）。標籤為「線性邊界」「多項式與 λ」：前者大學錄取兩特徵、sigmoid 機率與直線決策邊界；後者微晶片資料上的多項式特徵映射與 λ 正則化。兩階段共用同一教學流程圖拓撲與訓練微步驟。階段1 訓練特徵經 Z-score；階段2 不另做 Z-score。
_Avoid_: Tab、分頁（指 st.tabs 實作）；階段名寫成完整「正則化邏輯迴歸」當 radio 主標籤；側欄獨立「正則化邏輯迴歸」頁；兩階段各做不同節點拓撲

**訓練前預測（邏輯迴歸）**:
解鎖「開始訓練」前的概念關卡；兩個學習階段各兩題。階段「線性邊界」：sigmoid／機率意義，以及 Cost 依機率擬合、與 threshold 無關；階段「多項式與 λ」：為何多項式映射，以及 λ 變大時邊界／過擬合傾向。
_Avoid_: 僅一階段有關卡；硬鎖「必須先跟 Agent 說話才能訓練」

**K-Means 分群**:
非監督式分群教學頁的正式名稱；指定群數 K，依距離把樣本分成 K 群。側欄與 Ward's Method 同屬「機器學習 · 非監督式」；僅使用內建範例資料（不提供 ready.csv 切換），沒有 ready 亦不鎖頁。第一版為開箱探索：固定兩特徵、可調 K、看分群散點；不做訓練前預測解鎖；學習階段／關卡之後再加。
_Avoid_: KMeans（可作檔名／口語）、K 平均、以 ready.csv 當本頁資料來源；第一版就上訓練前預測殼

**Ward's Method（階層分群）**:
非監督式階層分群教學頁的正式名稱；以 Ward 最小變異法合併，並以樹狀圖觀察過程。側欄與 K-Means 分群同屬「機器學習 · 非監督式」；僅使用內建範例資料（不提供 ready.csv 切換），沒有 ready 亦不鎖頁。第一版為開箱探索：樹狀圖為主，並可切成 k 群畫 2D 散點（與 K-Means／真實群對照開關共用視覺語言）；不做訓練前預測解鎖；學習階段／關卡之後再加。
_Avoid_: Ward's method 當唯一標題、階層聚類當頁標題主文；以 ready.csv 當本頁資料來源；第一版就上訓練前預測殼；第一版只有樹狀圖、完全不能切群著色

**內建範例資料（非監督式分群）**:
K-Means 分群與 Ward's Method 兩頁第一版共用的主資料：兩特徵、可畫 2D、尺度設計為相近；另含教學用真實群標籤（**3** 個潛在群）。UI 固定使用這兩特徵（不提供欄位下拉）；第一版不露標準化開關。K-Means 的 K 與 Ward 切群數預設皆為 **3**。有明確教學需求再開對照／陷阱資料（類似 K-近鄰尺度對照）再講標準化，不預設為每頁各一份。
_Avoid_: ready.csv 作為本頁資料來源；第一版就強制兩頁分叉資料；高維主路徑；第一版欄位 multiselect；第一版標準化 checkbox；第一版預設故意設錯 K／切群數

**教學用真實群標籤**:
內建分群範例裡供著色／對照「潛在結構」的群欄；演算法只吃數值特徵，不把此欄當訓練輸入或監督答案。第一版圖上預設只顯示演算法分群結果；以開關才並排對照真實群（群編號各自獨立，不可假設同號同色）。
_Avoid_: 目標欄、標籤欄（未加「教學用／真實群」易與監督式 y 混淆）、y；一進頁就強制並排真實群主視覺；用同一色表把演算法群號與真實群號直接疊加當成對齊

**圖表探索**:
前處理側欄頁；以選對圖種關卡練習視覺化，資料來源固定為 Ready 分析就緒資料。
_Avoid_: 本頁切換 Working／Original；以 train／val／test 當本頁資料來源

**資料切分**:
前處理側欄頁的正式名稱；把 Ready 分析就緒資料拆成訓練／驗證／測試三份並寫出檔案。側欄在圖表探索之後（圖表仍只讀 Ready，不讀切分產物）。預設比例為訓練 60%／驗證 20%／測試 20%。有類別目標欄時預設做分層切分（鐵達尼為 Survived），使各份類別比例接近整體。與決策樹頁內的「訓練／驗證切分」不是同一概念。重建 Ready 會作廢既有切分產物（須重新切分）。「清除回雙表起點」亦一併清除切分產物。
_Avoid_: 訓練／驗證切分（指決策樹頁內靜默 holdout）、資料集切分、train-test split（作頁標題）；重建 Ready 或回雙表後仍沿用舊 train／val／test；暗示圖表探索讀的是 train／val／test；預設暗示為 70／15／15

**分層切分**:
資料切分時依目標類別比例抽樣，使訓練／驗證／測試中各類占比接近 Ready 整體。本專案前處理線於資料切分頁提供；有目標欄時預設開啟。
_Avoid_: 分層抽樣（可作口語）、stratify（作頁標題主文）；無目標欄時仍強制分層

**雙表起點**:
課堂整合線的起始狀態與協作區入口狀態：無 working／ready／切分產物。進「欄位與資料整合」時從內建雙表教材寫出雙表工作副本。「清除回雙表起點」刪除 Working／Original／切分／副本後回到此狀態。不提供單表 CSV 上傳進 Original／Working。
_Avoid_: 僅刪 working 卻留下 ready、train／val／test 或雙表工作副本仍稱為回到起點；以「資料上傳與預覽」或上傳 CSV 作為協作線起點；側欄再設僅名為「資料整合」的獨立頁才算起點完成

**內建雙表教材**:
課堂只讀的乘客表／航程表 CSV（`built-in-data/integration/`）。不得覆寫。進雙表起點頁時複製為雙表工作副本。
_Avoid_: Original、Working、雙表工作副本

**雙表工作副本**:
合併前 Agent 可改的兩份檔（`workspace/integration/passengers.csv`、`voyage.csv`）。雙表起點進頁即確保兩份存在。合併前只准對齊航程表鍵名，不得另存新檔。不是 Original，也不是 Working。畫面讀副本；「重新讀取雙表」重讀副本；「恢復內建雙表」刪副本後由進頁再次從教材寫出。資料整合寫入成功後刪除副本。
_Avoid_: working.csv、original.csv；直接改內建雙表教材；把副本叫 Original 或 Working；合併前對副本做鍵名以外的清理；合併前按鈕寫「重新讀取工作資料」或「回到原始資料」；靠 Agent 自行建立目錄或自創檔名

**欄位與資料整合（雙模態）**:
「AI 協作資料整理」側欄第一項；無 working.csv 時並排乘客表與航程表以便找合併鍵，並在本頁請 Agent 對齊鍵後做資料整合（主教學欄含訓練前預測（資料整合），控制為「重新讀取雙表」「恢復內建雙表」）；有 working.csv 時呈現單表欄位診斷（控制為「重新讀取工作資料」「回到原始資料」與「清除回雙表起點」）。無 Working 時的空狀態導引一律指本頁。
_Avoid_: 欄位與資料概覽（舊側欄名）；側欄另設僅名為「資料整合」的頁；用 phase 檔判斷整合前後（以有無 working 為準）；側欄第一項「資料上傳與預覽」；以「套用合併並寫入工作資料」按鈕寫入

**資料整合**:
在「欄位與資料整合」上由 Agent 執行的合併動作，不是側欄頁名。學生先請 Agent 對齊航程表鍵名（寫入雙表工作副本，可在關卡前；合併前不准做鍵名以外的清理），通過訓練前預測（資料整合）後再請 Agent 以 left 或 inner 合併，寫入 Original 原始資料與 Working 工作資料。未對齊鍵名不得合併。左表固定為乘客表、右表固定為航程表（不可對調）。課堂主線建議 left。此動作是 Agent 唯一被允許寫入 Original 的時刻。
_Avoid_: 把側欄頁名縮成「資料整合」；合併（作側欄頁標題）、join 頁；UI 對調左右表；以上傳 CSV 建立 Original／Working；「套用合併並寫入工作資料」按鈕

**訓練前預測（資料整合）**:
欄位與資料整合在雙表起點的兩題關卡；答對後 Agent 才被允許執行資料整合寫入 Original／Working。不鎖對齊鍵名。題意仍是先對齊鍵名、以及 left 會保留全部乘客；解鎖後 inner 與 left 皆可寫入。
_Avoid_: 解鎖套用（舊按鈕語意）；關卡通過前禁止改鍵名；關卡通過後仍禁止 inner 寫入

**Original 原始資料**:
資料整合寫入時與 Working 同批建立的重置來源；不直接修改。合併完成前不存在。
_Avoid_: 上傳後保留（舊說法）；暗示仍有單表上傳頁建立 Original；把尚未合併的單表或雙表工作副本當成 Original

**乘客表**:
鐵達尼整合教材的左表（畫面名稱）；對應內建乘客側欄位，含身分與 Survived 等。
_Avoid_: passengers 表（可作檔名／副標）、左表（未指明乘客時）

**航程表**:
鐵達尼整合教材的右表（畫面名稱）；對應內建航程側欄位，含 Ticket／Fare／Cabin／Embarked 等；鍵欄刻意為 `passenger_id`。
_Avoid_: voyage 表（可作檔名／副標）、右表（未指明航程時）

**資料轉換**:
前處理側欄頁的正式名稱；把同一語意的不同寫法收斂成約定標準值（如性別混寫）。頁內以欄位輔助資訊與同義提示協助選欄；寫回以 Agent 更新 working 為主；不是編碼、不是特徵工程。
_Avoid_: 編碼、特徵工程、正規化（指特徵縮放）；本頁 UI 主按鈕套用 mapping（第一版）；把同義提示當硬性必轉清單
