# Dataset Streamlit Shell

教學用 Streamlit 資料實驗室殼層：上傳／整理 CSV，並以內建範例走通監督式與非監督式演算法頁。

## Language

**K-近鄰分類**:
監督式分類教學頁的正式名稱；以距離找 k 個鄰居、多數決決定類別的實例型（懶惰）學習。側欄插在線性支持向量機與決策樹概念之間；僅使用內建範例資料（不提供 ready.csv 切換）。採訓練前預測殼（兩題解鎖＋Agent 提示），解鎖後主按鈕為「開始預測演示」並播放預測過程演進；不做類神經網路式實驗迴圈。
_Avoid_: KNN 分類（可作副標／口語）、K 最近鄰、k-NN、KNN 回歸；本頁主按鈕「開始訓練」

**學習階段（K-近鄰分類）**:
頁內用 horizontal radio 切換的教學段落（不用 tabs）。階段1 標籤「鄰居與投票」：建立找鄰居＋多數決；階段2 標籤「選擇 k」：以 k 為主角（可帶尺度相關觀察）。各階段各自兩題訓練前預測。
_Avoid_: Tab、分頁（指 st.tabs 實作）；階段1 標籤出現「k／超參數」當主菜

**k（K-近鄰分類）**:
要查詢的最近鄰居個數。階段1 固定且不在 UI 露出（固定為奇數預設）；階段2 以奇數步進的 slider 調整。第一版距離固定歐氏、投票為多數決（uniform）；不露 weights／metric 進階項。
_Avoid_: 階段1 的 k slider（含 disabled 顯示）；第一版強調平票處理流程

**內建範例資料（K-近鄰分類）**:
兩特徵、二元分類的教學用 CSV。階段1／2 以可畫 2D 決策邊界的資料為主；階段2 另具備刻意尺度失衡的對照，供關閉標準化時觀察距離被某一軸主導。
_Avoid_: ready.csv 作為本頁資料來源；第一版 multiclass／高維主路徑

**訓練前預測（K-近鄰分類）**:
解鎖主按鈕前的概念關卡。階段1：實例型（無 w）與多數決；階段2：k 過大趨向過度平滑／多數類，以及未標準化時大尺度特徵主導距離。解鎖後的主按鈕為「開始預測演示」（非「開始訓練」）。
_Avoid_: 類神經網路式實驗迴圈；硬鎖「必須先跟 Agent 說話才能訓練」；本頁主按鈕沿用「開始訓練」

**查詢點（K-近鄰分類）**:
用來示範預測的單一座標點；畫出其 k 個最近訓練鄰居（可連線），讓「鄰居與投票」可見。以圖上 click 指定；本頁因此採用可點選的互動圖（Plotly），不沿用其他 ML 頁的 matplotlib＋st.pyplot 主路徑。
_Avoid_: 第一版以 confusion matrix／classification report 當主視覺；強制全專案改 Plotly

**開始預測演示（K-近鄰分類）**:
本頁解鎖後的主按鈕文案；按下後 fit 鄰居池、畫決策邊界，並啟動預測過程演進。本頁對應區塊標題為「預測演示」；學生可見的解鎖／提示文案一併避開「開始訓練」。其他監督式頁仍用「開始訓練」。
_Avoid_: 本頁主按鈕、區塊標題或可見解鎖文案寫「開始訓練」／「##### 訓練」；為對齊本頁而改其他 ML 頁按鈕

**預測過程演進（K-近鄰分類）**:
按「開始預測演示」後播放的預測儀式：訓練集固定留作鄰居池；決策邊界全程顯示。以內建 **3 筆**示範查詢點（新資料、非訓練列）依序進來。對每一點：鄰居依距離一筆一筆進場並累加票數（查詢點改預測類別色＋短 caption 含票數）→ 多數決標類別 → 再下一點。已標示的新點以預測色留在圖上；**鄰居連線只顯示當下正在預測的那一筆**。播完後仍可 click 加演，**示範點與後續 click 點皆累積保留**；重新按「開始預測演示」可清空重跑。兩學習階段皆有。對齊懶惰學習的預測，不是學權重／降 Cost 的訓練迴圈。
_Avoid_: 訓練過程演進（本頁語意）；偽 epoch／Cost 曲線；對訓練集逐筆預測／leave-one-out 當主秀；第一版另備 holdout CSV（示範查詢點用固定座標即可）；單點內 k 條連線一次出現當唯一節奏；自動串超過 3 筆（第一版）；已預測新點的連線全部留在圖上；演進時隱藏決策邊界；click 後清掉示範點（第一版）

**卷積神經網路（CNN）**:
影像辨識觀念教學頁的正式名稱；以小視窗（kernel）在圖上滑動找局部特徵，再經活化與縮小後分類。敘事以指定觀念主軸影片為骨幹，開頭保留「圖片＝矩陣」前置；互動示範用來驗證觀念，不是實驗迴圈主體。
_Avoid_: CNN 介紹（可作檔名／口語）、Convolutional Neural Network 當頁標題主文、把 inductive bias 專名當第一版主菜

**學習階段（卷積神經網路）**:
頁內用 horizontal radio 切換的教學段落（不用 tabs）。六段依序為：圖片與矩陣；為什麼需要 CNN；卷積與特徵圖；ReLU 與 Pooling；CNN 整條流程；動手做 CNN。
_Avoid_: Tab、分頁（指 st.tabs 實作）；把六段併回「局部只改兩 tab」的舊結構

**觀念主軸影片（卷積神經網路）**:
DataMListic《CNNs - Explained》（video_id `YGILT182T6w`）。階段1不嵌入；階段2～6嵌入同一支片並附該段建議時間戳；正文為繁中整理，影片為對照。
_Avoid_: 影片僅備課不進學生 UI；硬鎖「必須看完整支才能往下」；第一版灌譯 inductive bias 專名

**訓練前預測（卷積神經網路）**:
階段6解鎖「開始訓練」前的兩題概念關卡：kernel／卷積在做什麼，以及 pooling 留下什麼。
_Avoid_: 階段6直接訓練無關卡；把「為什麼不用 Dense」或 ReLU 當解鎖硬門檻；硬鎖「必須先跟 Agent 說話才能訓練」

**史話補充（卷積神經網路）**:
Hubel & Wiesel 與 LeNet-5 等歷史／生物啟發內容；降級為可展開補充，不佔「為什麼需要 CNN」主線。
_Avoid_: 與影片動機並列當階段2正文主軸

**線性回歸**:
監督式回歸教學頁的正式名稱；側欄單一頁，內含單變量與多變量兩個學習階段。各階段僅用對應內建範例資料（固定欄位，不提供欄位下拉）；採訓練前預測殼；不做本頁保存模型 JSON／手動預測。
_Avoid_: 側欄分「單變量線性回歸」「多變量線性回歸」兩頁；把欄位選擇當成主路徑

**學習階段（線性回歸）**:
頁內用 horizontal radio 切換的教學段落（不用 tabs）。標籤為「單變量」「多變量」：前者餐廳獲利一特徵一線；後者房價多特徵（含 Z-score 縮放）。
_Avoid_: Tab、分頁（指 st.tabs 實作）；階段名寫成完整「單變量線性回歸／多變量線性回歸」當 radio 主標籤

**K-Means 分群**:
非監督式分群教學頁的正式名稱；指定群數 K，依距離把樣本分成 K 群。側欄與 Ward's Method 同屬「機器學習 · 非監督式」；僅使用內建範例資料（不提供 ready.csv 切換），沒有 ready 亦不鎖頁。
_Avoid_: KMeans（可作檔名／口語）、K 平均、以 ready.csv 當本頁資料來源

**Ward's Method（階層分群）**:
非監督式階層分群教學頁的正式名稱；以 Ward 最小變異法合併，並以樹狀圖觀察過程。側欄與 K-Means 分群同屬「機器學習 · 非監督式」；僅使用內建範例資料（不提供 ready.csv 切換），沒有 ready 亦不鎖頁。
_Avoid_: Ward's method 當唯一標題、階層聚類當頁標題主文；以 ready.csv 當本頁資料來源

**內建範例資料（非監督式分群）**:
K-Means 分群與 Ward's Method 兩頁第一版共用的主資料：兩特徵、可畫 2D；另含教學用真實群標籤。有明確教學需求再開對照／陷阱資料（類似 K-近鄰尺度對照），不預設為每頁各一份。
_Avoid_: ready.csv 作為本頁資料來源；第一版就強制兩頁分叉資料；高維主路徑

**教學用真實群標籤**:
內建分群範例裡供著色／對照「潛在結構」的群欄；演算法只吃數值特徵，不把此欄當訓練輸入或監督答案。
_Avoid_: 目標欄、標籤欄（未加「教學用／真實群」易與監督式 y 混淆）、y
